# Remove project trust

After uninstalling, remove the jastreamer certificate from the user and local-machine Trusted Root/Trusted Publishers stores using Windows certificate management. Confirm the published fingerprint is no longer present.
