# jastreamer 0.2 사용자 안내서

[English user guide](INSTRUCTION.md) · [설치 및 업데이트](INSTALL.ko.md) · [프로젝트 소개](README.ko.md)

음악 색인과 공용 대기열, 모든 재생 명령은 Server가 관리합니다. Server는 Linux 컨테이너 또는 네이티브 Windows에서 실행되고 모든 클라이언트가 보는 Web 화면을 제공하며, UPnP/DLNA 수신기와 선택적인 Google Cast 수신기, AirPlay 수신기(Linux 컨테이너 전용) 또는 지금 사용 중인 기기의 로컬 출력으로 소리를 보냅니다.

이 문서는 일상 사용법과 문제 해결을 다룹니다. 설치·업데이트·롤백은 [설치 안내서](INSTALL.ko.md)에 있습니다. 에이전트에게 설치나 업데이트를 맡길 때는 [AGENTS.md](AGENTS.md)부터 읽고, 비밀번호나 인증서를 에이전트 프롬프트에 붙여 넣지 마세요.

## 목차

- [용어](#glossary)
- [1. 최초 설정과 일상 사용](#1-최초-설정과-일상-사용)
- [2. 보관함: 스캔과 파일 검사](#library)
- [3. 대기열, 좋아요, 재생 순서](#queue)
  - [좋아요와 일회성 대기열 섞기](#likes) · [재생 순서와 반복](#playback-modes) · [재생 횟수와 많이 재생한 곡](#most-played)
- [4. 재생 기기](#outputs)
  - [이 기기: 로컬 오디오](#browser-output) · [Windows 네이티브 오디오](#windows-audio) · [휴대전화 화면](#phone-controls) · [Desktop 앱](#desktop) · [네이티브 Android](#android-controls) · [네이티브 iOS](#ios-controls) · [Google Cast](#cast)
- [5. 설정 살펴보기](#settings)
- [6. 진단과 로그](#diagnostics)
- [7. 문제 해결](#troubleshooting)

<a id="glossary"></a>
## 용어

| 용어 | jastreamer에서의 뜻 |
|---|---|
| Server | 음악을 색인하고 대기열을 보관하며 재생 명령을 내리는 프로그램입니다. 나머지는 모두 리모컨이거나 출력입니다. |
| Control | Server가 제공하는 Web 화면입니다. 브라우저, Desktop 앱, 네이티브 Android 앱, 네이티브 iOS 앱이 모두 같은 화면을 보여 줍니다. |
| 재생 기기 | 실제로 소리가 나는 곳입니다. UPnP/DLNA 수신기, Google Cast 수신기, AirPlay 수신기 또는 로컬 출력이며 **재생 기기** 선택기에서 고릅니다. |
| **이 기기** | 지금 보고 있는 페이지·앱이 제공하는 로컬 출력으로 `… (이 기기) [로컬 오디오]`처럼 표시됩니다. Server 대기열의 출력일 뿐 별도 대기열이 아닙니다. |
| 대기열 | Server 전체가 공유하는 재생 목록입니다. 순서와 중복 항목을 유지하고 Server를 재시작해도 남으며 최대 10,000곡까지 담깁니다. |
| 현재 곡 | 불러온 곡과 재생 위치입니다. 대기열 항목과 별도로 관리하므로 항목을 삭제하거나 대기열을 비워도 재생이 끊기지 않습니다. |
| **저장된 음악** | Android 앱의 오프라인 보관함·플레이리스트·기기 대기열입니다. 휴대전화에만 있고 Server가 필요 없습니다. |

<a id="1-first-setup-and-everyday-use"></a>
<a id="1-최초-설정과-일상-사용"></a>
## 1. 최초 설정과 일상 사용

1. **Server를 엽니다.** 완전한 사설 LAN 주소를 사용하세요. 기본 Linux 리스너는 `http://<server-LAN-IP>:8080/`, 기본 네이티브 Windows 리스너는 같은 주소의 18080 포트입니다. 최초 관리자 계정은 신규 설치에서만 만들며 비밀번호는 10자 이상이어야 합니다. 업데이트 뒤에는 기존 계정으로 로그인하고 최초 설정을 반복하거나 데이터를 초기화하지 마세요.
2. **언어를 고릅니다.** 기본값은 영어입니다. **Settings → 일반**의 **Language / 언어**에서 **English** 또는 **한국어**를 선택하세요. 메뉴 이름은 두 언어 모두 **Settings**입니다. 선택은 이 기기에만 즉시 적용되며 서버 설정을 저장하거나 재생 명령을 보내지 않습니다.
3. **음악 위치를 지정합니다.** **Settings → 보관함**에서 음악 폴더 경로(표준 Linux 컨테이너는 `/music`)를 확인하고 **설정 저장**을 누른 뒤 **지금 스캔**을 선택하세요. 이 명시적인 스캔 전에는 아무것도 나타나지 않습니다. 샘플을 포함한 새 설치는 `jastreamer-samples` 아래에 MP3 세 개를 두지만 테스트용 파일이며 자동으로 대기열에 담기거나 재생되지 않습니다. 호스트 폴더가 비어 있으면 재생할 곡이 없습니다. 설치 때 확인한 실제 호스트 경로에 음악을 넣고 다시 스캔하세요.
4. **필요하면 Google Cast를 켭니다.** **Settings → 재생·출력**에서 **Google Cast 출력**을 켜고 저장한 뒤 Server를 재시작하세요. `cast.enabled`가 `false`이거나 이전 설정에 없으면 계속 꺼진 상태입니다. 네트워크에 수신기가 보인다는 이유만으로 켜지 마세요.
5. **곡을 담습니다.** **보관함**과 **플레이리스트**를 탐색·검색해 **전체 재생**, **다음 순서에 추가**, **대기열 끝에 추가**를 사용하세요.
6. **재생 기기를 고르고 재생합니다.** 재생이 정지된 상태에서 **재생 기기**를 선택하세요. 방금 켠 수신기가 목록에 없으면 **재생 기기 다시 찾기**를 누릅니다. AirPlay가 PIN이나 암호를 요구하면 정지 상태에서 페어링하세요. 이후 재생/일시 정지, 정지, 이전 곡, 다음 곡과 수신기가 지원하는 탐색을 사용합니다.

지원 형식은 FLAC, MP3, WAV/WAVE, Ogg/Vorbis, Opus, M4A입니다. jastreamer는 원본 파일을 수정하거나 옮기거나 다시 쓰지 않습니다.

### 키보드와 미디어 키

- Escape는 열려 있는 창(곡 정보, 서버 경로 탐색기, AirPlay 설정 도움말, 플레이리스트 선택, 다운로드 상태, 재시작 확인)을 닫고 휴대전화의 펼친 플레이어를 접습니다.
- **Settings**의 탭 목록은 좌우 화살표와 Home·End 키로 이동할 수 있습니다.
- Web 화면에는 재생/일시 정지 전역 단축키가 없습니다. 화면의 플레이어를 사용하세요.
- 하드웨어 미디어 키는 네이티브 출력이 세션을 소유할 때 동작합니다. [Windows 네이티브 오디오](#windows-audio)는 Windows 시스템 미디어 제어를, [네이티브 Android 앱](#android-controls)은 Android 시스템 미디어 제어와 잠금 화면을 사용합니다. 브라우저 백엔드 로컬 오디오는 운영체제 미디어 제어에 등록하지 않습니다.

<a id="library"></a>
## 2. 보관함: 스캔과 파일 검사

**Settings → 보관함**에는 **음악 폴더**, **보관함 스캔**, **백그라운드 오디오 검증**이 있습니다. 폴더 변경 저장과 스캔 시작은 별도 동작입니다.

| 동작 | 하는 일 |
|---|---|
| **지금 스캔** | 증분 방식입니다. 처음에는 모든 음악을 읽고, 이후에는 폴더 목록에서 추가·누락 파일을 찾은 뒤 루트·경로·크기·수정 시각이 모두 같으면 저장된 메타데이터와 완료된 검증 결과를 재사용합니다. 신규·변경 파일만 분석하고 끝나지 않은 검사는 이어서 진행합니다. |
| **전체 다시 스캔** | 확인을 거쳐 변경되지 않은 파일까지 모든 파일의 메타데이터와 오디오 검증을 다시 수행합니다. 크기·수정 시각을 그대로 둔 채 파일을 교체했거나 완료된 검사를 다시 하고 싶을 때 사용하세요. 곡 ID, 좋아요, 재생 횟수, 플레이리스트, 대기열은 초기화하지 않습니다. |
| **백그라운드 오디오 검증** | 색인이 성공하면 검사가 필요한 파일을 Server의 FFmpeg로 한 번에 하나씩 끝까지 디코딩합니다. 재생 중이거나 다른 스캔이 진행되는 동안에는 일시 중지하며, 집계에는 재사용한 결과도 포함합니다. FLAC은 디코딩된 샘플 수와 STREAMINFO 체크섬이 있으면 그 값도 비교합니다. |

**보관함 스캔**은 최근 5건의 스캔을 기기 현지 시각 기준 시작·완료 시각과 함께 보여 줍니다. 이 표시 제한 때문에 이전 기록이 삭제되지는 않습니다. 변경 없는 파일의 실패·판정 불가 결과도 정상으로 바꾸지 않고 유지하며, Server를 재시작해도 결과가 남습니다.

색인 완료는 무결성 검사 통과를 뜻하지 않습니다. 실패한 검사와 검증할 수 없었던 파일은 음악 폴더 이름과 상대 경로와 함께 기록에 남습니다. 검사 엔진 부재, 지원하지 않는 형식, 시간 초과, 변경되거나 읽을 수 없는 파일은 결코 정상으로 보고하지 않습니다. 검사는 원본을 복구·덮어쓰기·삭제하거나 보관함·대기열에서 자동으로 지우지 않으며, Server 재시작 뒤에도 진행을 이어갑니다.

<a id="queue"></a>
## 3. 대기열, 좋아요, 재생 순서

대기열은 Server 전체가 공유합니다. 모든 Control이 같은 목록을 보고, 순서와 중복 항목이 유지되며 재시작 뒤에도 남지만 재시작만으로 재생이 이어지지는 않습니다. Google Cast도 이 대기열 하나를 사용하며 Cast autoplay를 끈 상태로 미디어를 불러온 뒤 Play를 명시적으로 보냅니다. 수신기 그룹과 gapless 재생은 지원하지 않습니다.

### 곡 삭제와 대기열 비우기

- 재생 중이거나 일시 정지·선택된 곡을 포함해 어떤 항목이든 삭제할 수 있습니다. **현재 재생** 중인 곡과 재생 위치는 대기열과 별도로 유지되므로 항목을 삭제해도 정지 명령이 가거나 오디오가 다시 시작되지 않습니다.
- 대기열 제목 옆의 **대기열 모두 삭제**는 확인을 거쳐 이전·현재·다음 항목을 모두 지웁니다. **좋아요 섞어 담기**와는 별개이며, 두 동작 모두 원본 음악 파일·좋아요·저장된 플레이리스트를 지우지 않습니다.
- 재생 중인 곡은 자연스럽게 끝나거나 사용자가 재생 명령을 내릴 때까지 계속 재생됩니다. 대기열이 비어 있어도 일시 정지·재개와 지원되는 위치 탐색을 쓸 수 있고, 소리를 멈추려면 **정지**를 누릅니다.
- **한 곡 반복**은 해당 항목이 대기열에서 삭제되었거나 대기열이 비어 있어도 현재 곡이 자연스럽게 끝나면 다시 재생하며, 삭제한 항목을 되살리지 않습니다. **다음 곡**은 반복 설정과 무관하게 남은 순차·랜덤 순서를 따릅니다. **반복 없음**이나 **전체 반복**에서는 남은 항목이 없으면 곡이 끝난 뒤 정지합니다. 전체 삭제 뒤 곡을 추가하거나 **다음 순서에 추가**를 눌러도 자동으로 재생하지 않습니다.
- Server를 재시작하면 현재 곡 선택과 저장된 위치는 유지하되 삭제한 항목을 복구하거나 자동 재생하지 않습니다. Android **저장된 음악**은 자체 로컬 대기열을 그대로 유지합니다.

### 폴더 탐색과 목록 작업

**보관함 → 폴더**의 **상위 폴더**는 같은 음악 루트 안에서 한 단계 위로 이동합니다. 루트에서는 **목록으로**(그리고 **폴더** 탭)가 음악 루트 목록으로 돌아갑니다. 폴더 이동만으로는 대기열이나 재생이 바뀌지 않습니다.

폴더 작업은 현재 폴더와 **모든 하위 폴더**에서 조건에 맞는 재생 가능한 곡을 경로 순서로 포함합니다. 중간 폴더에 직접 담긴 곡이 없거나 목록이 여러 페이지여도 하위 곡을 모두 포함하며, 형제 폴더나 다른 음악 루트는 포함하지 않습니다. 화면 탐색 자체는 계속 한 단계씩 진행합니다.

| 동작 | 결과 |
|---|---|
| **전체 재생** | 기존 재생을 정지하고 대기열을 선택한 곡들로 교체한 뒤 재생합니다. |
| **다음 순서에 추가** | 현재 곡 바로 뒤에 넣습니다. 현재 곡이 없으면 대기열 맨 앞에 넣습니다. |
| **대기열 끝에 추가** | 기존 항목 뒤에 붙입니다. 두 추가 동작 모두 재생을 끊거나 자동으로 시작하지 않습니다. |
| **저장된 플레이리스트에 추가** | 기존 저장 목록에 붙이거나 새 목록을 만들며 대기열은 그대로 둡니다. |

탐색과 목록 작업은 한 줄의 도구 모음을 함께 사용하므로 좁은 화면에서는 좌우로 스크롤하세요. **대기열 끝에 추가**는 목록·아래 화살표 아이콘으로 재생 대기열을 바꾸고, **저장된 플레이리스트에 추가**는 북마크·더하기 아이콘으로 저장 목록 선택 창을 엽니다.

<a id="likes"></a>
### 좋아요와 일회성 대기열 섞기

- 보관함, 플레이리스트, 대기열, 곡 정보의 하트 버튼으로 좋아요를 설정·해제합니다. 네 화면 모두 좋아요는 채워진 하트, 해제는 윤곽 하트로 표시하며 대기열의 중복 항목과 열려 있는 곡 정보 창까지 함께 갱신됩니다. 좋아요를 바꿔도 대기열 순서와 재생은 변하지 않습니다.
- 좋아요는 계정별 비공개 목록이 아닌 Server 공용 상태이며 다시 스캔하거나 Server를 재시작해도 유지됩니다.
- 보관함의 **좋아요**를 선택하면 좋아요한 곡만 탐색할 수 있고 검색·페이지 구분도 그대로 적용됩니다.
- **대기열**의 **좋아요 섞어 담기**는 현재 페이지나 검색 결과와 관계없이 재생 가능한 좋아요 곡 전체를 무작위 순서로 대기열 끝에 추가합니다. 기존 순서와 중복 항목은 그대로 두고 현재 재생을 끊지 않으며 정지 상태에서 자동 재생하지 않습니다. 사용할 수 없는 곡은 제외합니다. 담을 곡이 없거나 대상이 10,000곡을 넘거나 추가 후 대기열이 10,000곡 제한을 넘으면 일부만 담지 않고 오류를 표시합니다.
- 공용 대기열에 한 번 담는 기능이며 저장된 플레이리스트나 실시간 필터가 아닙니다. 이후 좋아요를 바꿔도 이미 담긴 곡은 그대로이고 플레이리스트가 만들어지지도 않습니다. 결과를 보관하려면 대기열의 **플레이리스트로 저장**을 사용하세요. 이전에 만든 좋아요 셔플 목록을 포함한 기존 플레이리스트는 그대로 유지됩니다.

<a id="playback-modes"></a>
### 재생 순서와 반복

- 플레이 바에는 **순차 재생 / 랜덤 재생**과 **반복 없음 / 전체 반복 / 한 곡 반복**이 각각 독립적으로 있습니다. 데스크톱 화면에서는 재생 버튼 옆에 있고, 휴대전화의 간결한 플레이 바에는 **재생 모드** 버튼이 있으며 펼친 플레이어에는 두 제어가 그대로 보입니다.
- **순차 재생**은 표시된 대기열 순서를 따릅니다. **랜덤 재생**은 표시 순서를 바꾸거나 중복 항목을 합치지 않고 별도의 무작위 순회를 사용합니다. 한 순회에서 각 항목은 한 번씩 나오며, 랜덤을 켜거나 대기열을 교체하면 새 순회가 시작됩니다. 명시적인 **다음 순서에 추가**는 이후 다른 곡을 대기열 끝에 붙여도 계속 다음 차례를 유지하고, 일반 추가 묶음은 내부 순서를 섞어 현재 순회의 끝에 붙습니다.
- **반복 없음**은 마지막 곡에서 멈추고, **전체 반복**은 새 순회를 시작하며 랜덤 모드에서는 순서를 다시 만듭니다. **한 곡 반복**은 곡이 자연스럽게 끝났을 때만 적용되므로 **다음 곡**을 누르면 다음 곡으로 넘어갑니다. 미디어 오류를 무한히 반복하지는 않습니다.
- 랜덤 모드의 **이전 곡**은 다시 무작위로 고르지 않고 현재 순회에서 지나온 순서를 따릅니다. 탐색 가능한 곡을 5초 넘게 재생했다면 **이전 곡**은 지금 곡의 처음으로 돌아갑니다.
- 모드를 바꿔도 정지 상태에서 재생이 시작되거나 현재 곡이 다시 시작되거나 재생 위치·저장된 플레이리스트가 바뀌지 않습니다. Server의 모드는 공용 대기열과 모든 출력에 적용되고 다른 Control에도 동기화되며 재시작 뒤에도 유지되지만 자동 재생하지는 않습니다. Android 저장된 음악은 독립된 로컬 모드를 사용합니다.

<a id="most-played"></a>
### 재생 횟수와 많이 재생한 곡

- **보관함 → 많이 재생한 곡**에는 한 번 이상 집계된 재생 가능한 곡이 횟수 내림차순으로 표시되고 각 행에 **N회**가 함께 나옵니다. 전체 순위를 매긴 결과에 검색과 페이지 구분을 적용하며, 곡 정보의 **재생 횟수**도 같은 값을 보여 줍니다(아직 없으면 0회).
- 계정별 비공개 통계가 아니라 Server 전체 공용 통계입니다. 한 재생 세션에서 확인된 재생 시간이 30초에 도달하면 1회로 집계하고, 60초 미만 곡은 길이의 절반이 기준입니다. 길이를 모르면 30초를 기준으로 합니다.
- 집계는 재생 기기의 재생 상태와 위치 진행을 연결해 판단합니다. 사람이 실제로 스피커 소리를 들었다는 증거는 아닙니다.
- 일시 정지 후 이어 들으면 같은 세션의 재생 시간이 누적되며 한 세션을 두 번 집계하지 않습니다. 일시 정지 시간, 탐색으로 건너뛴 시간, 진행이 멈춘 구간, 재생 상태를 확인할 수 없는 공백은 제외합니다. 같은 곡을 다시 재생해 기준을 다시 넘기면 횟수가 하나 더 늘어납니다.
- 이 기능이 설치된 시점부터 집계하며 과거 기록을 되살리지 않습니다. 확정된 횟수는 다시 스캔하거나 Server를 재시작해도 유지되지만 기준에 도달하지 않은 재생 시간은 재시작 뒤 복원하지 않습니다. Android **저장된 음악**만으로 들은 재생은 포함하지 않습니다.
- 통계를 살펴봐도 대기열이 바뀌거나 재생이 시작되지 않습니다. 횟수는 음악 파일 태그가 아닌 Server 데이터베이스에 저장하며 원본 음원을 변경하지 않습니다.

### 앨범 아트와 대기열 행 동작

- 플레이 바의 앨범 아트를 누르면 **대기열**로 이동합니다. 곡 정보를 열거나 재생을 시작하지는 않습니다.
- 보관함의 `(i)`와 대기열의 앨범 아트는 재생 없이 곡 정보를 엽니다. 대기열 앨범 아트는 마우스를 올리거나 키보드로 포커스하면 큰 `(i)`를 표시하고, 터치 화면에서는 항상 표시합니다.
- 대기열 각 행 오른쪽 첫 번째 동작이 삼각형 재생 버튼이며, 그 버튼만 해당 항목을 재생합니다.

<a id="outputs"></a>
## 4. 재생 기기

재생이 정지된 상태에서 **재생 기기**를 선택하세요. 기기를 켜거나 새로 연결한 뒤에는 **재생 기기 다시 찾기**를 누릅니다.

| 출력 | 사용 가능한 곳 | 참고 |
|---|---|---|
| UPnP/DLNA | Linux·네이티브 Windows Server | SSDP(UDP 1900)로 자동 검색합니다. |
| Google Cast | Linux·네이티브 Windows Server | 기본값은 꺼짐이며 설정에서 켜고 재시작해야 합니다. 선택한 어댑터에서 mDNS(UDP 5353)가 필요합니다. [Google Cast](#cast) 참고. |
| AirPlay | Linux 컨테이너 Server 전용 | 패키지에 포함된 송신 프로그램과 FFmpeg가 필요하며 정지 상태에서 페어링합니다. 네이티브 Windows Server는 AirPlay를 켤 수 없습니다. |
| **이 기기** `[로컬 오디오]` | 브라우저, PWA, Desktop 앱, 네이티브 Android | 지금 사용 중인 기기에서 Server 대기열을 재생하는 출력입니다. [이 기기](#browser-output) 참고. |

기능은 수신기마다 다릅니다. 실제 장비에서 소리가 나는지와 필요한 제어가 되는지 직접 확인하세요.

<a id="browser-output"></a>
### 이 기기: 로컬 오디오

이 절은 브라우저, PWA, Linux Desktop, 기본 브라우저 백엔드를 쓰는 Windows Desktop에 적용됩니다. 네이티브 엔진은 [Windows 네이티브 오디오](#windows-audio)나 [네이티브 Android](#android-controls)를 참고하세요. 네이티브 iOS에는 로컬 재생이 없습니다. Server 모드의 로컬 출력은 모두 **로컬 오디오**로 표시되며 Server 대기열을 재생합니다.

1. 재생을 정지하고 **재생 기기**에서 **(이 기기)**가 붙은 항목(예: `Windows · Chrome (이 기기) [로컬 오디오]`)을 선택합니다. 휴대전화에서는 간결한 플레이어를 펼치면 선택기가 나옵니다.
2. 곡을 고르거나 기존 대기열을 그대로 두고 **재생**을 누릅니다. 브라우저가 오디오를 막으면 그 페이지의 **재생 허용**을 직접 누르세요. 대기 중이던 요청이 이미 실패했다면 오류를 닫고 다시 재생하면 됩니다. 명령을 조용히 재전송하는 일은 없습니다.
3. 일시 정지, 정지, 이전 곡, 다음 곡과 지원되는 탐색을 평소처럼 사용합니다. 브라우저는 Server 대기열의 출력이며 별도 로컬 대기열을 만들지 않습니다.

**페이지 진입 시 자동 선택.** 기존에 선택된 출력이 오프라인이고 재생이 정지·사용 불가 상태라면 **이 기기**를 한 번 자동으로 선택합니다. 대기열·현재 곡·저장된 위치는 그대로이며 **재생**은 직접 눌러야 합니다. 그사이 출력이나 재생 상태가 바뀌거나 기존 출력이 다시 온라인이 되면 전환하지 않습니다. Android는 저장된 음악이나 다른 Server의 재생 소유권을 가져오지 않으며, 이미 열려 있는 페이지에서 연결이 끊겼다고 계속 재등록하지도 않습니다.

**로컬 음량.** 선택된 **이 기기**가 현재 페이지·앱 소유라면 **로컬 음량**으로 0–100%를 조절할 수 있습니다(휴대전화에서는 플레이어를 펼치세요). 브라우저 오디오, Windows 네이티브 PCM 게인, Android의 Server 모드 Media3 음량만 바꾸며 운영체제 전체 음량이나 네트워크 수신기, 다른 기기에는 영향을 주지 않습니다. 네이티브 제어에는 그에 맞는 Desktop·APK 빌드가 필요합니다.

**로컬 출력 이름.** 브라우저의 기본 이름은 페이지가 알 수 있는 OS·브라우저 정보이며 컴퓨터 호스트 이름이나 휴대전화에 지정한 이름이 아닙니다. 네이티브 앱은 `Windows · jastreamer`, `Android · jastreamer`를 고정 기본값으로 사용합니다. 출력 선택기 옆 연필 버튼 **로컬 오디오 출력 이름 변경**으로 `작업실 PC` 같은 별칭을 저장하고, **기본 이름 사용**을 누르거나 빈 값으로 저장하면 자동 이름으로 돌아갑니다. 이름은 UTF-8 기준 80바이트까지이고 한글은 글자당 여러 바이트를 씁니다.

별칭은 이 브라우저 프로필에서 해당 Server UUID와 포트를 포함한 정확한 origin별로 저장됩니다. 다른 브라우저·프로필, 비공개 탐색, 교체된 Server로 따라가지 않으며 브라우저 저장소를 지우면 함께 사라집니다. 저장에 실패하면 저장된 것처럼 표시하지 않고 오류를 알립니다.

**(이 기기)** 표시는 해당 출력을 등록·소유한 페이지에만 붙습니다(예: `작업실 PC (이 기기)`). 같은 계정이어도 다른 페이지·기기에서는 `작업실 PC`로만 보입니다. 아직 등록하지 않은 페이지도 자기 로컬 출력을 이 표시와 함께 제공하는데, 이는 남의 수신기에 붙은 표시가 아닙니다. 등록 전에 이름만 저장해도 출력이 등록·선택되지는 않습니다. 등록된 출력의 이름을 바꾸면 다른 클라이언트에도 반영되지만 출력 ID·대기열·재생·출력 선택은 바뀌지 않습니다. 별칭은 표시 이름이며 검증된 하드웨어 식별자가 아닙니다.

**브라우저 백엔드의 성격.** 제어는 같은 origin의 인증된 HTTP JSON을, 음원은 브라우저 오디오 요소의 HTTP(S) GET/Range를 사용합니다. UPnP·HLS·DASH·WebRTC가 아닙니다. 실제 스피커·헤드폰은 브라우저와 운영체제가 고르므로 하드웨어 선택기, WASAPI/ASIO 엔진, 독점 모드, 비트 퍼펙트 보장이 없고 지원 형식은 브라우저 디코더에 따라 달라집니다. 변환에는 변환 허용 설정과 FFmpeg가 필요하며 변환된 WAV 스트림은 탐색할 수 없습니다.

**재생을 담당하는 페이지는 열어 두세요.** 그 페이지를 닫거나 새로고침하면 출력이 해제되고, 등록이 끊기면 대기열은 유지한 채 출력을 사용 불가로 표시합니다. 다시 들어오면 위 조건에서 자동 선택될 수 있고, 그렇지 않으면 필요에 따라 정지한 뒤 **이 기기**를 다시 선택하고 재생하세요. 다른 Control을 닫아도 재생 중인 브라우저는 멈추지 않으며 네트워크 출력은 Control 종료와 무관합니다. Windows Desktop의 **X**는 창을 닫지 않고 숨깁니다([Desktop 앱](#desktop) 참고). 일반 브라우저와 PWA의 백그라운드·잠금 화면 재생은 브라우저와 운영체제에 따라 달라 보장되지 않으며, Desktop도 절전·로그아웃 뒤의 재생을 보장하지 않습니다.

<a id="windows-audio"></a>
### Windows 네이티브 오디오

호환되는 Windows Desktop 패키지와 호환 Server Web 화면에서 선택적으로 사용하는 WASAPI 출력입니다. 이전에 게시된 패키지의 기능이 아니며 오프라인 보관함이나 별도 대기열을 추가하지 않습니다.

1. 재생을 정지하고 진행 중인 작업이 끝날 때까지 기다립니다. 출력으로 **이 기기**를 선택하세요. **Windows 오디오 설정**은 이 기기가 선택된 출력일 때만 출력 제어 옆에 나타납니다. **로컬 오디오 백엔드**를 **브라우저(기본값)**에서 **Windows 네이티브(선택 사용)**로 바꿉니다.
2. 이름이 표시된 **Windows 오디오 엔드포인트**를 고르면 Windows 기본 장치와 관계없이 항상 그 장치로 출력하고, 현재 기본 장치 이름이 함께 표시되는 **Windows 기본 장치 따르기**를 고르면 그때그때 Windows 기본 장치를 따릅니다. 기기를 새로 연결했다면 출력 목록을 새로 고치세요. 지정한 엔드포인트가 사라지면 오류로 처리하며 다른 장치로 대체하지 않습니다.
3. **독점 모드**를 **끔**으로 두면 엔드포인트의 믹스 형식을 쓰는 WASAPI 공유 모드, **켬**으로 두면 정확한 형식의 독점 모드를 요청합니다. 기기 점유, 지원하지 않는 형식, Windows 독점 정책 거부는 오류로 남으며 조용히 공유 모드로 되돌아가지 않습니다.
4. 엔드포인트·모드를 바꿔도 **이 기기** 출력은 그대로이고 다음 곡부터 적용됩니다. **브라우저(기본값)**와 **Windows 네이티브(선택 사용)**를 전환하면 로컬 출력이 교체되며, 이 기기가 선택된 출력이었다면 자동으로 다시 선택합니다. 재생은 직접 누를 때까지 정지 상태입니다. 일시 정지·로드 상태도 엔드포인트를 점유하므로 설정을 바꾸기 전에 정지해야 합니다.

**브라우저(기본값)**를 선택한 동안에는 엔드포인트와 **독점 모드** 선택기를 사용할 수 없습니다. 브라우저 오디오는 항상 Windows 기본 장치로 공유 모드 출력합니다.

독점 모드에서는 앱 음량이 100%로 고정되고 **로컬 음량** 조절이 제공되지 않으므로 음량은 DAC나 앰프에서 조절하세요. 환경설정은 EXE 옆 `user-data`에 보존됩니다.

Server가 다시 시작되면 Windows 재생 연결이 끊깁니다. 앱은 로그인 문제가 아니라 연결이 끝났다고 알리고 Server를 다시 열 때 같은 알림을 반복하지 않습니다. **이 기기**를 다시 선택하세요.

**패널 읽는 법.** 패널은 **요청한 경로**와 **실제 활성 경로**(엔드포인트, 모드, 샘플 레이트, 채널, 컨테이너 너비, 유효 비트 정밀도)를 구분해 보여 줍니다. **로컬 음량**은 이 앱의 PCM 게인만 바꿉니다.

**애플리케이션 경로가 비트 투명 전송 조건을 충족함** 표시는 Server가 미변환으로 명시한 무손실 원본, 변하지 않은 레이트·레이아웃·정밀도, 게인 1.0(앱 음량 100%), 실제 독점 모드가 모두 성립할 때만 나타납니다. 원본 상태를 알 수 없거나 손실 디코딩, 공유 모드, 샘플 변경이 있으면 조건을 충족하지 않습니다. 이 표시는 애플리케이션 경로에 대한 것이며 드라이버·DSP·DAC 동작이나 물리적인 비트 퍼펙트 출력을 검증한 것이 **아닙니다**.

번들 FFmpeg 디코더는 FLAC·MP3·AAC/M4A·Vorbis·Opus·WAV를 지원하지만, 특히 독점 모드에서는 출력 기기가 디코딩된 형식을 거부할 수 있습니다. 인증된 Desktop 세션이 같은 origin의 음원 바이트를 제한된 크기로 가져오며 오디오 helper에는 자격 증명이나 미디어 URL을 전달하지 않습니다. 원본 파일은 다시 쓰지 않습니다.

Windows 시스템 미디어 제어는 현재 곡, 사용 가능한 앨범 아트, 실제 타임라인을 표시합니다. 재생·일시 정지·정지·이전·다음과 지원되는 탐색은 Server로 전달되며 별도 대기열을 만들거나 Server 명령을 우회하거나 선택된 다른 네트워크 출력을 제어하지 않습니다. 로컬 재생이 끝나거나 정지하거나 소유권을 잃으면 제어가 사라집니다.

같은 Server에서 WebView를 새로고침해도 Desktop 소유자와 등록이 유효하면 네이티브 재생이 유지됩니다. **X**는 창만 숨기고, 트레이 **종료**는 다른 네트워크 출력에 정지 명령을 보내지 않고 로컬 엔드포인트와 등록만 해제합니다. helper나 등록이 최종적으로 끊기면 자동으로 재등록·재생하지 않습니다. **이 기기**를 다시 연결해 직접 재생하거나 정지 상태에서 **브라우저**로 되돌리세요. Windows 빌드, 실제 엔드포인트의 공유·독점 동작, 미디어 키 연동과 실제 소리는 Windows 장비에서 따로 확인해야 하며 디코더 해시나 UI 상태는 물리적 재생의 증거가 아닙니다.

<a id="phone-controls"></a>
### 휴대전화 화면

휴대전화 전용 화면은 iPhone·iPod 브라우저와 사용자 에이전트에 Android와 Mobile이 함께 표시되는 Android 브라우저에서만 자동으로 선택됩니다. iPad와 Android 태블릿, 폭이 좁은 데스크톱 창은 기존 표준 화면을 유지합니다.

- 위쪽 헤더에는 계정 이름과 로그아웃이 남고, 아래쪽 네 탭은 **보관함**, **플레이리스트**, **대기열**, **Settings**입니다. 일반 브라우저에서는 jastreamer 로고를 표시하며 Android·Desktop·iOS 앱 안에서만 중복되는 Web 헤더·사이드바 로고를 숨기되 계정 제어는 그대로 둡니다.
- 간결한 플레이어에는 재생/일시 정지, 정지, **재생 모드** 버튼이 바로 있습니다. 펼치기 화살표를 누르면 탐색, 이전 곡, 다음 곡, 랜덤·반복, 출력 선택과 새로 고침, 필요할 때의 AirPlay 페어링이 나타나며 앨범 아트를 누르면 **대기열**로 이동합니다.
- 주요 재생·탐색·곡 동작 버튼은 최소 44×44픽셀 터치 영역을 제공하고 헤더·플레이어·하단 탐색은 기기의 safe area를 반영합니다.
- 다른 하단 탭을 선택하면 재생을 멈추지 않고 펼친 플레이어만 접히며, 키보드 포커스가 플레이어 안에 있으면 Escape로도 접힙니다. 높이가 짧거나 가로 방향 화면에서는 펼친 패널이 스크롤되고, 패널이 덮은 본문은 닫기 전까지 조작되지 않습니다.

선택 사항인 설치형 Web 앱(PWA)은 홈 화면 바로가기에서 같은 화면을 열고 같은 Server 대기열을 제어합니다. 항상 Server에 연결할 네트워크가 필요하며 service worker·cache·오프라인 재생은 제공하지 않습니다. 설정 화면에는 앱 설치 카드가 없으므로 브라우저의 ‘앱 설치’ 또는 ‘홈 화면에 추가’ 메뉴를 사용하세요. 신뢰할 수 있는 HTTPS 요구 사항과 브라우저별 절차는 [휴대전화 PWA 설치](INSTALL.ko.md#pwa)를 참고하세요. 선택 사항인 Desktop 앱에는 PWA 설치가 필요하지 않습니다.

<a id="desktop"></a>
### Desktop 앱

Desktop 앱은 **서버 재생** 화면으로 시작하며 **지금 발견한 서버**와 **최근 연결한 서버**를 별도 카드 영역으로 보여 줍니다. 카드에는 서버 이름·주소와 확인된 연결 상태가 나오지만, 저장된 기록만으로 지금 연결할 수 있다는 뜻은 아닙니다. 카드의 연결 버튼을 누르거나 **주소로 직접 연결**에서 HTTP(S) 루트 주소를 입력하세요. 취소나 Escape는 연결하지 않고 창을 닫으며 입력 초안을 유지합니다. 검색·화면 이동·연결만으로 재생이 시작되지는 않습니다.

Desktop 앱에는 독립 저장 음악 플레이어나 로컬 재생 카드가 없습니다. 연결한 뒤 공유 Web 화면의 **이 기기** 출력은 Server 보관함과 대기열을 사용합니다.

Windows에서 **X**를 누르면 창이 알림 영역 트레이로 숨고 연결과 로컬 재생은 유지됩니다. 트레이 아이콘을 클릭·더블 클릭하거나 컨텍스트 메뉴의 **JASTREAMER 열기**를 선택하거나 앱을 다시 실행하면 같은 창이 복원됩니다. 완전히 끝내려면 트레이 아이콘을 우클릭해 **종료**를 선택하세요. 이때 앱의 로컬 출력은 해제되지만 네트워크 출력에 정지 명령을 보내지는 않습니다. 업데이트 전에는 이 방법으로 종료하세요. 트레이 아이콘을 만들 수 없는 환경에서는 **X**가 기존처럼 앱을 종료하며 Linux의 창 닫기 동작은 그대로입니다.

<a id="android-controls"></a>
### 네이티브 Android 화면

네이티브 Kotlin 앱은 공유 Web 화면 바깥에 서버 선택, Server가 제어하는 휴대전화 출력, 독립적인 **저장된 음악** 플레이어를 더합니다. [호환 Server·Web 버전, 설치와 APK 업데이트 규칙](INSTALL.ko.md#android)은 PWA 설치와 별개입니다.

**연결하기**

- 앱 홈에는 **서버 재생**과 **로컬 재생** 카드가 있습니다. **로컬 재생**은 연결이나 로그인 없이 **저장된 음악**을 열고, 카드의 보조 **다운로드** 동작은 전송 관리자를 엽니다. **서버 재생**은 **지금 발견한 서버**와 **최근 연결한 서버**를 구분해 보여 주며, 주소를 직접 넣으려면 **주소로 직접 연결**에서 HTTP(S) 루트 주소를 입력하고 **확인 후 연결**을 선택하세요. 새로 실행해도 자동 연결하지 않고 Server 식별자 검증은 항상 수행합니다.
- Server 모드의 네이티브 헤더에는 선택한 Server와 포트를 포함한 완전한 origin이 표시됩니다. **서버**는 Server가 제어하는 네트워크 출력이나 저장된 음악 재생을 멈추지 않고 선택 화면으로 돌아갑니다. 저장 항목의 Server UUID가 달라지면 기존 세션을 조용히 재사용하지 않고 거부합니다.
- 시스템 바·키보드 여백은 네이티브 셸에서 한 번만 적용합니다. 뒤로 가기는 직접 연결 창이나 키보드를 먼저 닫고, 이어서 현재 Web 또는 저장된 음악의 화면 구조를 따릅니다. Web 기록 끝에서는 서버 선택으로, 서버 선택과 저장된 음악의 최상위 화면에서는 홈 카드로 돌아가며 홈에서 다시 누르면 앱을 나갑니다. 회전해도 현재 화면과 저장하지 않은 Web 입력이 유지됩니다. 백그라운드에서 돌아오면 페이지를 보여 주기 전에 Server를 다시 확인하지만, 확인 실패가 **저장된 음악**을 막지는 않습니다.
- Cookie와 Web 저장소는 Server UUID와 포트를 포함한 완전한 origin별로 격리됩니다. **최근 서버 목록에서 제거**는 바로가기만 지우며 로그아웃도, 격리 프로필이나 저장된 음악 삭제도 아닙니다. 세션을 끝내려면 Control에서 로그아웃하세요. 로그아웃하면 해당 Server의 미완료 가져오기는 중지되지만 이미 확정된 음악은 기기에 남습니다.
- 네이티브 언어 선택과 **Settings → 일반 → Language / 언어**에서 영어·한국어를 지원합니다. Web에서 바꾼 언어는 페이지 로딩이 끝나거나 화면을 나가거나 일시 중단할 때 네이티브 화면에 반영됩니다.

**재생 소유권**

- 하나의 Media3 서비스가 이 휴대전화를 없음·Server·저장된 음악 중 하나의 소유자로만 운용합니다. Server가 재생을 소유한 상태에서 저장된 음악을 시작하거나, 로컬 음악이 소유한 상태에서 휴대전화를 Server 출력으로 지정하면 명시적인 전환 확인을 요구합니다. 이전 소유자를 먼저 정지하며 대기열은 복사하지 않고 이전 소유자의 늦은 명령도 전환 경계를 넘지 못합니다.
- Server 모드에서는 `Android · jastreamer (이 기기) [로컬 오디오]` 또는 저장한 별칭을 직접 선택한 뒤 **재생**을 누르세요. 대기열과 명령은 Server가 소유하며 Android 시스템 미디어 제어의 재생·일시 정지·정지·이전·다음과 지원되는 탐색도 Server로 전달됩니다. 저장된 음악 모드에서는 같은 제어가 로컬 파일과 기기 대기열에 적용됩니다.
- 네이티브 셸은 화면 닫기·백그라운드 전환·서버 변경·재실행만으로 Play나 Stop을 보내지 않고, 서비스가 살아 있으면 Activity·WebView 재생성 뒤에도 재생이 유지됩니다. 프로세스 종료나 등록의 최종 상실은 Server 소유 재생을 정지합니다. 로컬 보관함·대기열·재생 위치는 재시작 뒤 복원하지만 자동 재생하지 않습니다. 위에서 설명한 오프라인 출력 자동 선택은 Server 페이지를 새로 열 때만 시도되며 저장된 음악의 소유권을 가져오거나 진행 중인 사용자 요청을 앞지르지 않습니다.
- 지원 형식은 Media3·Android 디코더에 달려 있습니다. 원본 가져오기는 디코더 호환이나 비트 퍼펙트 출력을 보장하지 않으며, Server 재생에는 아래의 선택적 USB 경로가 추가됩니다. Server 모드의 일시적 음원 오류는 제한된 범위에서 복구하고 최종 오류는 무한 재시도하지 않고 표시합니다.
- 확인된 Server의 현재 최상위 문서에만 Server 출력과 명시적 가져오기용 제한된 네이티브 인터페이스를 제공합니다. Cookie, 자격 증명, 임의 네이티브 호출, 파일 시스템 탐색, 임의 URL 다운로드는 노출하지 않으며 외부 탐색, 새 창, 파일 선택기, 그 밖의 Web 다운로드와 네이티브 권한 요청은 계속 차단합니다. 일반 브라우저와 Linux Desktop은 브라우저 오디오를 유지하고, 호환 Windows Desktop에는 별도의 선택적 오디오 bridge가 있으며, 네이티브 iOS에는 Android 인터페이스도 로컬 재생도 추가되지 않습니다.

**USB 비트 퍼펙트 오디오 (Android 14 이상)**

이 앱의 PCM을 Android 믹싱·리샘플링·볼륨 조정 없이 연결된 USB DAC으로 보내는 선택 기능입니다. 기본값은 꺼짐이고 이 휴대전화의 Server 재생에만 적용되며, **저장된 음악**은 계속 표준 Android 출력을 사용합니다.

1. 재생을 정지하고 진행 중인 작업이 끝날 때까지 기다린 뒤 휴대전화 출력(`Android · jastreamer (이 기기) [로컬 오디오]`)을 선택하세요. **휴대전화 오디오 설정**은 이 기기가 선택된 출력일 때만 출력 제어 옆에 나타납니다.
2. **USB 비트 퍼펙트**를 **켬**으로 바꾸세요. 이 설정은 특정 Server가 아니라 이 휴대전화의 앱에 저장되며 다음 곡부터 적용됩니다. 휴대전화 출력 등록은 그대로 유지되고 **재생**을 누를 때까지 재생은 정지 상태로 남습니다. 일시 정지되거나 로드된 오디오는 먼저 정지해야 변경할 수 있습니다.
3. 켜져 있는 동안 앱 볼륨은 100%로 고정되고 **로컬 볼륨** 제어는 제공하지 않습니다. 음량은 DAC이나 앰프에서 조절하세요.

필요한 조건은 패널이 알려 줍니다. Android 14 이상, 연결된 USB 오디오 기기, 비트 퍼펙트 믹서를 제공하는 기기가 필요합니다. 어느 하나라도 없으면 항목은 이유와 함께 그대로 표시되고 일반 재생은 영향을 받지 않습니다.

**패널 읽는 법.** 패널은 **요청한 경로**와 **실제 활성 경로**를 나눠 USB 기기, 모드, 샘플 레이트, 채널 수, 컨테이너 폭, 유효 비트 정밀도, PCM 표현을 보여 줍니다. 실제 값은 요청이 아니라 Android가 실제로 연 오디오 트랙과 프레임워크가 보고한 믹서 속성에서 가져옵니다.

**조용한 대체는 없습니다.** USB 기기가 해당 곡의 형식을 비트 퍼펙트 스트림으로 받지 못하거나 기기가 사라지면, 그 곡은 Android 믹서로 조용히 바뀌지 않고 오류로 실패합니다. 그런 곡을 일반적으로 들으려면 재생을 정지하고 **USB 비트 퍼펙트**를 끄세요.

**애플리케이션 경로가 비트 투명 전송 조건을 충족함**은 다음이 모두 성립할 때만 나타납니다. Server가 미변환으로 명시한 무손실 원본, 샘플 레이트·레이아웃·정밀도 유지, 게인 1.0(앱 볼륨 100%), 그리고 실제로 비트 퍼펙트인 믹서입니다. 이 표시는 애플리케이션 경로에만 해당하며 드라이버·DSP·DAC 동작이나 물리적 비트 퍼펙트 출력을 **검증하지 않습니다**.

**정밀도 한계.** 이 클라이언트가 재생하는 형식에서 Media3는 16비트 PCM을 출력하므로 24비트 원본은 출력 전에 16비트로 줄어듭니다. 그런 곡도 비트 퍼펙트 믹서를 사용할 수 있지만 패널은 16비트와 정밀도가 줄었다는 이유를 표시하며, 비트 투명이라고 주장하지 않습니다. CD 레이트 FLAC과 WAV 같은 16비트 무손실 원본은 조건을 충족할 수 있습니다.

USB 기본 설정은 설정을 끌 때, USB 기기를 분리할 때, Server 재생 소유권이 끝날 때(연결 해제, **저장된 음악**으로 전환, 등록의 최종 상실), 재생 서비스가 종료될 때 해제됩니다.

**저장된 음악**

- **저장된 음악**은 Server HTML·네트워크·로그인 없이 앱의 **보관함 / 플레이리스트 / 대기열 / Settings**를 엽니다. 보관함은 **앨범**으로 시작하며 **아티스트 / 장르 / 폴더 / 곡 / 좋아요**, 검색, 모음 상세 탐색을 제공합니다. 장르는 저장된 메타데이터를 사용하고 좋아요는 Server가 아닌 이 기기에 속합니다. 완료된 가져오기는 메타데이터와 앨범 아트를 함께 보관하며 로그아웃, 세션 만료, 계정 상실, Server·프로필 제거, 구형이거나 연결할 수 없는 Server와 무관하게 사용할 수 있습니다.
- 곡과 모음 작업은 익숙한 **재생 / 다음에 재생 / 끝에 추가 / 저장 목록에 추가 / 곡 정보** 흐름을 따르고, 선택은 현재 분류와 검색 결과를 따릅니다.
- 로컬 플레이리스트를 만들고 이름·순서·항목을 바꾸거나 지워도 Server 플레이리스트는 그대로입니다. 목록 항목 제거나 로컬 목록 삭제만으로 오디오 파일이 지워지지 않습니다. 로컬 대기열은 순서와 반복 항목을 보존하므로 특정 항목부터 재생하거나 항목을 이동·제거하고 대기열을 로컬 플레이리스트로 저장할 수 있습니다. 항목을 추가해도 재생이 시작되거나 재생 위치가 초기화되지 않습니다. 탐색, 이전·다음, 셔플, 반복은 Server 대기열과 독립적입니다.
- 로컬 미니 플레이어에서도 정지 상태나 빈 대기열에서도 **순차 재생 / 랜덤 재생**과 반복을 바로 조작할 수 있고, 펼친 플레이어와 대기열도 같은 로컬 설정을 보여 줍니다. 이 제어는 재생을 시작하거나 표시된 대기열을 재정렬하지 않으며 Server의 재생 모드를 바꾸지 않습니다.
- 저장된 음악은 Control과 같은 탐색 구조, 어두운 배경과 녹색 강조, 접근성 이름이 붙은 아이콘 제어를 사용합니다. 미니 플레이어는 계속 보이고 그 위로 탐색, 이전·다음, 셔플, 반복, 대기열, 곡 정보 제어가 펼쳐지며 큰 앨범 아트나 재생 바를 중복 표시하지 않습니다. 넓은 화면에서는 곡 정보와 제어를 좌우로 배치합니다. 펼치거나 접어도 탐색 화면·검색어·스크롤 위치·대기열·재생 소유권이 유지되고, 휴대전화에서는 제어를 펼친 동안 뒤쪽 탐색 영역의 입력이 차단됩니다.
- **폴더**는 앱 전용 지속 저장소 안의 실제 디렉터리입니다. 폴더를 만들고 이름을 바꾸고 이동하며, 선택한 곡을 옮기고, 다운로드 위치를 고르고, 같은 이름 충돌을 덮어쓰기 없이 처리합니다. 이동 뒤에도 안정된 로컬 ID, 플레이리스트 항목, 대기열 항목, 재생 위치가 유지되고 중단된 저장소 작업은 기록을 바탕으로 정리한 뒤 다음 보관함 변경을 허용합니다. NAS 폴더나 다른 앱의 파일은 탐색·변경하지 않습니다.
- **기기에서 삭제**는 확인 뒤 선택한 로컬 오디오를 영구 삭제하고 로컬 플레이리스트·대기열 참조를 갱신합니다. 로컬 플레이리스트만 삭제하면 오디오는 남습니다. 현재 재생 중인 곡이 포함되면 **재생 종료 후 삭제** 또는 **지금 정지하고 삭제**를 고르며, 보류 삭제는 재생이 파일을 놓을 때까지 기다립니다.

**다운로드**

- 호환되는 Control과 Android 앱은 곡·앨범·플레이리스트·폴더에 **다운로드** 버튼을 제공합니다. 저장 위치와 함께 **원본**(검증한 원본 바이트) 또는 Server가 변환을 지원할 때의 **공간 절약**(AAC-LC 256 kbps M4A)을 확인하세요. 다운로드는 재생을 시작하거나 공용 Server 대기열을 바꾸지 않습니다. 플레이리스트 가져오기는 순서와 중복을 보존하는 일회성 스냅샷이며 이후 Server 편집을 따라가지 않습니다.
- Control의 **폴더** 화면에서는 보관함 루트, 하위 폴더, 현재 열린 폴더를 다운로드할 수 있습니다. 하위 경로까지 최대 10,000곡을 스냅샷해 선택한 로컬 폴더로 가져오며 Server의 디렉터리 구조를 복제하거나 플레이리스트를 만들지 않습니다. 이후 Server 변경을 반영하려면 직접 다시 가져와야 합니다.
- 요청이 접수되면 Server 화면에 진행 상태가 바로 열립니다. **다운로드** 항목은 현재 문서에서 시작한 작업의 받은 용량, 곡 수, 대기 이유, 완료·실패를 보여 줍니다. 진행 중인 다운로드 버튼은 상태를 다시 열고 **다시 다운로드**는 새 확인 절차를 시작합니다. 앱을 다시 실행해 복원된 작업까지 모두 보려면 네이티브 다운로드 관리자를 여세요.
- 기본 정책은 데이터 제한이 없는 비모바일 연결을 기다립니다. 대기 중인 작업에서 **다운로드 네트워크 설정**을 누르면 Server 화면에서 바로 설정할 수 있습니다. 과금망·모바일 데이터를 쓰려면 네이티브 확인 창에서 직접 승인하거나 관리자의 **데이터 제한 또는 모바일 데이터 허용**을 켜야 하며 취소만으로는 허용되지 않습니다. 로밍과 연결 불가 상태는 계속 차단되고, 접근 가능한 로컬 Server를 쓸 때 인터넷 연결 검증을 요구하지는 않습니다.
- 네이티브 **다운로드** 관리자는 준비, 전송, 검증, 로컬 가져오기, 일부 실패, 일시 정지·재개, 취소, 저장 위치 변경을 다룹니다. 진행 중인 작업은 시작한 Server와 로그인 주체에 묶입니다. v1 다운로드 기능이 없는 Server에서는 새 가져오기를 시작할 수 없지만 평소처럼 제어할 수 있고 이미 저장된 음악에도 영향이 없습니다.
- **다운로드 기록 제거**는 확인 뒤 완료·일부 저장·취소·실패 기록 하나를 지우고, **완료된 기록 지우기**는 그런 기록을 모두 지우되 진행 중·일시 정지 작업은 남깁니다. 두 동작 모두 저장된 음악·앨범 아트·폴더·플레이리스트·대기열을 그대로 두므로, 진행 중인 작업은 먼저 취소한 뒤 기록을 지우세요. 호환 Control의 상태 화면도 새로 고치면 제거된 기록을 함께 반영합니다.

Control의 PWA 설치 안내는 브라우저용이며 네이티브 Android 앱에는 PWA 설치가 필요하지 않습니다. 특히 암호화되지 않은 HTTP에서는 신뢰하는 Server만 사용하세요.

<a id="ios-controls"></a>
### 네이티브 iOS 화면

SwiftUI 앱은 같은 Server Web 화면을 감쌉니다. 현재 제공 범위는 설치 가능한 휴대전화 릴리즈가 아닌 [소스와 CI](INSTALL.ko.md#ios)입니다.

- 서버 전용 선택 화면은 **지금 발견한 서버**와 **최근 연결한 서버**를 구분합니다. 주변 서버 카드는 검색 검증을 통과한 항목이고, 최근 카드는 연결할 때 다시 확인하며 저장되어 있다는 이유로 온라인이라고 표시하지 않습니다. **주소로 직접 연결**을 누르면 네이티브 입력창이 열려 HTTP(S) 루트 주소를 넣고 **확인 후 연결**을 선택할 수 있습니다. 취소해도 입력 초안은 남고, 검증 중이었다면 그 시도를 먼저 취소합니다. 새로 실행하면 자동 연결하지 않고 선택 화면에 머뭅니다.
- 헤더에는 선택한 Server와 포트를 포함한 완전한 origin이 표시됩니다. **서버 변경**은 네트워크 출력의 재생을 멈추지 않고 선택 화면으로 돌아갑니다. 백그라운드에서 돌아오면 페이지를 보여 주기 전에 Server UUID를 다시 확인하며, 식별자나 네트워크 오류가 있으면 이전 페이지와 키보드에 접근할 수 없습니다.
- Cookie와 Web 로컬 저장소는 확인된 Server UUID와 정규화한 scheme·host·port로 구분한 named profile을 사용하므로 포트가 다르면 세션도 다릅니다. 최근 항목을 지우는 것은 목록만 바꾸므로 세션을 끝내려면 Control에서 로그아웃하세요.
- **뒤로**는 사용할 수 있는 Web 기록을 따라가고 **새로 고침**은 페이지를 다시 불러옵니다. 화면을 스크롤하면 키보드가 자연스럽게 내려갑니다. 회전해도 실행 중인 페이지와 저장하지 않은 입력이 유지됩니다. 높이가 작은 키보드 화면에서는 뒤로·새로 고침 바가 숨지만 서버 변경과 언어 선택은 그대로 쓸 수 있습니다.
- 네이티브 언어 선택과 **Settings → 일반 → Language / 언어**에서 영어·한국어를 지원합니다. JavaScript bridge 없이 페이지 로딩, 백그라운드 복귀, 네이티브 화면 전환 시점에 Web 언어를 읽어 반영합니다. 네이티브 언어를 바꾸면 Web 페이지를 다시 불러오므로 저장하지 않은 편집을 먼저 마치세요.
- 네이티브 셸은 종료·백그라운드 전환·서버 변경에서 Play나 Stop을 보내지 않습니다. Web 미디어 로드를 차단하며 로컬 재생 진입점, 네이티브 오디오 엔진, 오프라인 재생, 명령 대기열이 없습니다. 외부 탐색, 새 창, 다운로드, 파일 선택기, 네이티브 미디어·기기 권한 요청도 계속 차단하므로 로컬 오디오나 파일 업로드가 필요하면 일반 브라우저를 사용하세요.

검색이 되지 않으면 로컬 네트워크 접근 권한, Wi-Fi·멀티캐스트, VPN 경로를 확인하거나 완전한 주소를 직접 입력하세요. 특히 암호화되지 않은 HTTP에서는 신뢰하는 Server만 사용하세요.

<a id="cast"></a>
### Google Cast 미디어와 완료 판단 범위

Cast 직접 스트리밍은 검사한 codec, sample rate, channel과 해당되는 경우 bit depth 메타데이터를 바탕으로 보수적으로 선택합니다. 직접 전송 원본은 모두 일치하는 codec이 확인되고 sample rate가 양수이며 mono 또는 stereo여야 합니다.

| 원본 | 직접 스트리밍 허용 범위 |
|---|---|
| FLAC | 96 kHz, 1–24비트까지 |
| MP3, Ogg/Vorbis, Ogg/Opus, M4A/AAC | 48 kHz까지 |
| LPCM WAV | 48 kHz, 1–16비트까지 |

메타데이터가 없거나 맞지 않는 원본과 위 범위를 벗어난 형식은 호환된다고 가정하지 않습니다. 미디어 변환을 켜고 FFmpeg를 설정했다면 대신 탐색할 수 없는 44.1 kHz stereo 16비트 WAV 스트림을 사용하며 원본 파일은 변경하지 않습니다. 직접 스트리밍은 이 변환을 피할 뿐 수신기의 비트 퍼펙트 출력을 보장하지 않습니다.

Cast 제어는 지속 TLS 연결을 유지하며 자신이 실행한 애플리케이션·미디어 세션을 소유합니다. 대기열은 그 미디어에 대한 명시적인 `FINISHED` 상태를 받아야 진행하며 EOF, 빈 상태, `BUFFERING`, `ERROR`는 완료로 보지 않습니다. 일시 정지·탐색·허용 형식은 수신기에 따라 다릅니다.

<a id="settings"></a>
## 5. 설정 살펴보기

| 탭 | 항목 |
|---|---|
| **일반** | 언어, 서버 이름, 데이터 디렉터리, 계정 비밀번호 |
| **네트워크** | HTTP/HTTPS 수신 설정, 접근 규칙, 네트워크 어댑터, 검색·조회 주기, Server 음원 주소 |
| **보관함** | 음악 폴더, 스캔, 백그라운드 오디오 검증 |
| **재생·출력** | Google Cast, AirPlay와 설정 도움말, FFmpeg, 음원 변환 |
| **진단** | 재생·파일 검사 기록과 CSV 보고서 다운로드 |

탭을 바꿔도 저장하지 않은 편집 내용은 유지되고 아무것도 저장되지 않으며, 재시작·스캔·재생 명령도 일어나지 않습니다. **설정 저장**과 **변경 취소**는 모든 서버 설정 탭에 함께 적용되고 언어·계정 동작은 별도입니다. 숨은 탭에 유효하지 않은 값이 있으면 저장 전에 해당 탭과 입력으로 이동합니다. 재시작 안내와 설정 충돌 안내는 모든 탭에서 보입니다. 좁은 화면에서는 탭 목록을 좌우로 스크롤하세요.

### 서버 경로와 네트워크 선택

- 음악 폴더, 데이터 디렉터리, HTTPS 인증서·개인 키, FFmpeg, **AirPlay 송신 프로그램 경로** 옆의 **찾아보기**는 이 브라우저의 컴퓨터가 아니라 로그인한 서버의 파일 시스템을 탐색하는 창을 엽니다. Windows는 접근 가능한 드라이브를 보여 주고 절대 UNC 공유 경로를 받으며, Linux·NAS는 `/`에서 시작하고 컨테이너는 마운트된 경로만 노출합니다. 목록에서 심볼릭 링크와 Windows 재분석 지점은 제외하되 직접 경로 입력은 계속 쓸 수 있습니다.
- 루트, **상위 폴더** 또는 절대 디렉터리 경로로 이동하세요. **선택**은 편집 중인 필드만 바꾸고 **취소**는 기존 값을 유지합니다. 설정을 명시적으로 저장한 뒤 스캔하세요. 데이터 디렉터리를 골라도 기존 데이터베이스와 앨범 아트는 이동하지 않으니 저장 위치를 바꾸고 재시작하기 전에 데이터를 따로 옮기세요.
- **서버 네트워크 어댑터**에는 Server의 실제 어댑터 이름과 IP·프리픽스가 표시됩니다. **자동**은 `network.interfaces`를 비우고, 개별 선택은 직접 입력한 이름도 유지합니다. 사용할 수 없는 어댑터와 주소도 표시되지만 UPnP·Google Cast 검색용으로 새로 선택할 수는 없습니다. Cast mDNS는 선택한 인터페이스의 UDP 5353을 사용합니다.
- **재생 기기가 음원을 가져올 Server URL**은 `media.base_url`이며, 수신기가 Server에서 음원을 가져올 주소를 알려 줍니다. 같은 주소로 Control을 제공할 수도 있지만 이 설정은 수신 주소·포트·브라우저 URL을 바꾸지 않습니다. 보통은 비워 두어 Server가 자동으로 고르게 하세요. **서버의 네트워크 주소에서 선택**은 감지된 Server IP와 현재 활성화된 HTTP(S) 수신 포트를 조합해 초안만 채우므로 적용하려면 설정을 저장해야 합니다. 이 목록은 수신기 접근 가능 여부를 검사한 결과가 아니므로 부적합한 VPN·컨테이너 주소는 고르지 마세요. 직접 입력할 때는 수신기 LAN에서 실제로 접근할 수 있는 Server 주소와 활성 수신 포트를 쓰고, 원격 수신기에 `localhost`나 클라이언트 PC 주소를 쓰면 안 됩니다. 명시적인 값이나 리스너 주소가 우선하며, Cast는 Server에서 수신기가 mDNS로 알린 Cast 포트에도 접근할 수 있어야 합니다.
- 경로 탐색이나 어댑터·IP 선택만으로는 저장·재시작·스캔·재생이 일어나지 않습니다. 편집 내용을 직접 저장하세요. 리스너·저장 위치·네트워크 변경에는 재시작이 필요할 수 있습니다.

### AirPlay 설정 도움말

**AirPlay 송신 프로그램 경로** 옆의 **AirPlay 설정 도움말**은 필요한 송신 프로그램을 설명할 뿐입니다. 소프트웨어를 설치하거나 AirPlay를 켜거나 임의 경로를 검증하거나 수신기를 제어하지 않습니다. 지원되는 Linux `amd64`·`arm64` Server 컨테이너에는 `/usr/local/bin/jastreamer-airplay`, 고정된 pyatv 0.18.0을 쓰는 Python 3.12, FFmpeg가 하나의 일치하는 실행 환경으로 들어 있습니다. 네이티브 Windows Server는 Linux 경로나 임의의 helper 경로를 입력해도 AirPlay를 지원하지 않습니다.

별도로 설치하는 송신 프로그램은 `main`의 임의 버전이 아니라 설치한 Server와 같은 릴리즈의 어댑터 소스·의존성 파일을 사용하고, jastreamer와 일치하는 어댑터·helper 규약을 구현하며 호환되는 의존성을 갖춰야 합니다. `atvremote`, Python 실행 파일, pyatv 단독 설치, Shairport Sync 같은 수신기 소프트웨어는 대체 경로가 될 수 없습니다. 도움말 창은 [어댑터 소스](https://github.com/furyheimdall/jastreamer/blob/main/apps/server/internal/airplay/helper.py), [고정된 AirPlay 의존성](https://github.com/furyheimdall/jastreamer/blob/main/packaging/server/requirements-airplay.txt), [pyatv 0.18.0 소스](https://github.com/postlund/pyatv/tree/v0.18.0)의 전체 URL을 표시하지만 이 `main` 링크가 설치한 릴리즈와의 호환성을 보장하지는 않습니다. 일반 브라우저에서는 링크를 따라갈 수 있고 Desktop 앱은 외부 창을 막을 수 있으므로 열리지 않으면 표시된 URL을 복사해 브라우저에서 여세요. 지원되는 Linux 송신 경로를 바꿨다면 저장한 뒤 Server를 재시작하세요.

### 저장한 설정 적용과 서버 재시작

저장한 설정에 재시작이 필요하면 **Settings**에 안내와 **서버 재시작** 버튼이 표시되고, 화면을 다시 열어도 안내가 남습니다. 저장하지 않은 편집은 먼저 저장하거나 되돌리세요. 확인하면 재생을 정지하고 저장된 설정으로 Server를 다시 시작합니다. 계정·대기열·플레이리스트는 유지되며 재생은 자동으로 재개되지 않습니다.

주소가 그대로면 Control이 새 Server 실행에 다시 연결된 것을 확인한 뒤 완료를 표시합니다. 주소가 바뀌면 안내된 새 주소를 여세요. 실패나 시간 초과를 완료로 표시하지 않으며 재시작 명령을 자동으로 다시 보내지 않습니다. 데이터 디렉터리 변경은 이 버튼으로 적용할 수 없으므로 기존 데이터를 따로 옮긴 뒤 Server를 직접 재시작하세요. 재시작 API가 없는 이전 Server에는 수동 재시작 안내가 표시됩니다.

<a id="diagnostics"></a>
## 6. 진단과 로그

**Settings → 진단 → 재생 및 파일 검사 기록**에는 Server에 보관되는 공용 이력이 있습니다. **결과 유형**(**재생 기기 보고** 또는 **파일 검사**)이나 재생 기기로 필터링하세요. 재생 기기 보고에는 기기 이름과 ID, 곡, 오류 코드, 제공된 재생 위치, 펼쳐 볼 수 있는 구조화된 상세 정보가 담기며, 이전 로그에서 가져온 기록은 ID만 있을 수 있습니다. 최신 5,000건을 보관하고 기록을 보는 동작은 재생을 바꾸지 않습니다.

PC나 휴대전화 웹 브라우저에서 **CSV 다운로드**를 누르면 화면에 보이는 페이지만이 아니라 현재 필터와 일치하는 보관 기록 전체가 저장됩니다. UTC 시각, 결과, 곡·음악 폴더·상대 경로, 오류 코드와 메시지, 구조화된 상세 정보가 들어가고 재생 기기 보고에는 기기·재생 식별자도 남습니다. UTF-8 BOM으로 스프레드시트의 한글 인식을 돕고 수식처럼 시작하는 값은 텍스트로 처리해 실행되지 않게 합니다. 이 내보내기는 보관된 실패·판정 불가 이력이며 모든 파일의 정상 판정 증명서가 아니고, 기록을 지우거나 원본 파일·재생 상태를 바꾸지도 않습니다. Android·iOS·Desktop 내장 앱은 기존 다운로드 제한을 유지하고 일반 브라우저를 안내하므로 필요하면 그 브라우저에서 따로 로그인하세요.

**로그 위치.** Server 진단 로그는 UTC 시각으로 콘솔과 `<data_dir>/logs/server.log`에 함께 기록됩니다. 표준 Linux 컨테이너 경로는 `/var/lib/jastreamer/logs/server.log`이며 기존 데이터 마운트 안에 있어 컨테이너를 교체해도 남습니다. `server.log`와 `server.log.1`–`server.log.3`을 각각 최대 5 MiB로 순환 보관합니다. 정상적인 로컬 출력 활동은 등록별로 최대 30초에 한 번 요약하고 반복되는 요청 실패는 기록 빈도를 제한합니다. 이벤트는 생성된 출력·재생·명령 ID로 연결하며 자격 증명, 쿠키, 미디어 URL, 파일 이름, 출력 표시 이름은 기록하지 않습니다. POSIX 시스템에서는 로그 디렉터리를 0700, 파일을 0600 권한으로 만들고 Windows에서는 데이터 디렉터리 접근 권한을 Server 계정으로 제한하세요. 영구 로그를 시작할 수 없으면 Server 콘솔이나 Compose 로그를 확인하세요. 데이터 디렉터리를 HTTP로 공개하지 마세요.

**Android 재생 오류 보고.** 이 진단에는 대응하는 Server와 APK 버전이 필요하므로 [업데이트 절차](INSTALL.ko.md#upgrade)에 따라 호환 Server를 먼저 업데이트하세요. 실패한 네이티브 명령과 최종 재생 오류 보고에는 Media3 오류 코드·이름, 발생 시각, 재생 위치, 원인 예외 타입, 길이가 제한된 코드 기반 호출 스택이 들어가고 확인 가능한 HTTP 상태와 코덱·오디오 출력 숫자 오류 코드도 포함됩니다. 예외 메시지 원문, 미디어 URL, 파일 경로, 자격 증명은 제외합니다. Server는 받은 보고를 별도 ADB 수집 없이 기존 순환 로그에 자동 보관합니다. 오프라인 업로드 대기열은 아니므로 전달 전에 프로세스가 종료되거나 Server 연결이 끊기면 수집되지 않을 수 있고 이미 유실된 예외는 복원하지 못합니다. 저장된 음악의 오류는 기기 내 로컬 진단에만 남고 자동 업로드하지 않습니다.

**문제를 보고할 때**는 Server 버전, 정확한 이미지 다이제스트 또는 Windows ZIP SHA-256, Server 플랫폼·아키텍처, 관련 로그, 수신기 모델을 포함하세요. 비밀번호·쿠키·인증서·개인 키는 제거하고 원본 진단 문구는 그대로 두세요.

<a id="troubleshooting"></a>
## 7. 문제 해결

### Server와 Control 접속

| 증상 | 예상 원인 | 확인·조치 |
|---|---|---|
| Web 화면이 열리지 않음 | Server가 실행되지 않거나 포트가 막힘 | Windows Server 콘솔과 TCP 18080, 또는 Linux `/healthz`, Compose 로그, 설정된 리스너와 TCP 8080/8443 방화벽 허용을 확인 |
| Desktop 앱이 Server를 찾지 못함 | mDNS가 차단됨 | mDNS UDP 5353을 허용하거나 **주소로 직접 연결**에 완전한 Server URL 입력 |
| 휴대전화 화면이 나타나지 않음 | 기기가 휴대전화로 인식되지 않음 | iPhone·iPod 브라우저이거나 사용자 에이전트에 Android와 Mobile이 함께 표시되는지 확인. iPad·Android 태블릿과 폭이 좁은 데스크톱 창은 의도적으로 표준 화면 유지 |
| 휴대전화 Web 앱 설치 항목이 없음 | origin이 신뢰된 HTTPS가 아니거나 브라우저가 설치를 지원하지 않음 | 브라우저의 설치 메뉴를 사용하고 [PWA 설치](INSTALL.ko.md#pwa)를 따름. 사설 LAN HTTP origin은 설치할 수 없음 |
| Android에서 WebView 미지원 표시 | 시스템 WebView·Chrome이 너무 오래됨 | Android System WebView 또는 Chrome을 업데이트한 뒤 앱을 다시 열어 격리 Server 프로필·Server 출력·새 가져오기를 복구. 공용 쿠키로 대체하지 않으며 이미 저장된 음악은 계속 사용 가능 |
| Android가 Server를 찾지 못하거나 재접속하지 못함 | 네트워크 격리 또는 식별자·TLS 오류 | Wi-Fi, mDNS UDP 5353, VPN·기기 격리, 앱 네트워크 접근을 확인하고 완전한 URL을 직접 입력. 식별자·TLS 오류는 우회하지 말 것. 이 실패가 이미 저장된 음악을 막지는 않음 |
| 설정이 저장되지 않음 | 설정 파일에 쓸 수 없음 | Windows에서는 EXE 옆 파일, Linux에서는 config 폴더와 `server.json`을 Server 계정/UID 10001이 쓸 수 있는지 확인 |
| 비밀번호를 잊음 | — | Server를 멈추고 같은 config·data를 연결한 유지보수 컨테이너에서 `jastreamer-server --reset-password USER --config /etc/jastreamer/server.json` 실행. 새 비밀번호는 프롬프트에만 입력 |

### 보관함

| 증상 | 예상 원인 | 확인·조치 |
|---|---|---|
| 보관함이 비어 있음 | 루트가 다르거나 읽을 수 없거나 아직 스캔하지 않음 | Windows 폴더 또는 Linux `/music` 마운트가 설정된 보관함 루트인지, Server 계정/UID 10001이 읽을 수 있는지, 스캔이 완료됐는지 확인. 번들 `jastreamer-samples`도 명시적 스캔이 필요 |
| 파일을 교체했는데 이전 메타데이터가 보임 | 크기·수정 시각이 같아 증분 스캔이 기존 결과를 재사용 | **전체 다시 스캔** 실행 |
| 파일 검사가 실패하거나 검증되지 않음 | FFmpeg 부재, 지원하지 않는 형식, 시간 초과, 변경·손상된 파일 | **Settings → 진단**에서 폴더 이름과 상대 경로를 확인해 파일이나 FFmpeg 설정을 수정. jastreamer는 원본을 복구·삭제하지 않음 |

### 재생 기기와 재생

| 증상 | 예상 원인 | 확인·조치 |
|---|---|---|
| 재생 기기가 보이지 않음 | 검색 트래픽이 차단됨 | UPnP는 SSDP UDP 1900을 허용. Google Cast는 선택한 인터페이스의 mDNS UDP 5353과 Server에서 수신기가 알린 Cast 포트로의 TCP 접근이 필요. Linux AirPlay는 mDNS UDP 5353과 호스트 네트워크가 필요. Wi-Fi 기기 격리 해제 |
| 기기를 골랐는데 재생되지 않음 | 수신기가 Server에서 음원을 가져올 수 없거나 형식을 지원하지 않음 | Server→수신기 제어·스트림과 수신기→Server 미디어 HTTP(S) 통신을 허용. **재생 기기가 음원을 가져올 Server URL**은 보통 비워 두고, 필요하면 수신기에서 접근 가능한 Server 주소만 지정. 지원되지 않는 Cast 원본은 FFmpeg를 설정한 뒤 변환 허용 |
| **이 기기**에서 소리가 없거나 연결이 끊김 | 브라우저 자동 재생 차단, 음소거, 등록 상실 | 표시되면 **재생 허용**을 누르고 브라우저·OS 음소거와 출력 경로를 확인하며 재생 담당 페이지를 열어 둘 것. 새로고침이나 등록 만료 뒤에는 필요하면 정지하고 **이 기기**를 다시 선택해 직접 재생. 디코더·통신 오류는 보존 |
| Windows 네이티브 오디오가 연결이 끝났다고 알림 | Server가 재시작되었거나 등록을 해제함 | 로그인은 유효하므로 **이 기기**를 다시 선택. [Windows 네이티브 오디오](#windows-audio) 참고 |
| 독점 모드가 실패하거나 **로컬 음량** 조절이 사라짐 | 기기 점유, 지원하지 않는 형식, Windows 정책 거부 — 또는 독점 모드가 활성 상태 | 거부는 오류로 남으며 공유 모드로 대체하지 않음. 독점 모드에서는 음량이 100%로 고정되므로 DAC에서 조절 |
| Cast에서 FLAC 끝부분을 탐색한 뒤 실패 | 독립적으로 재현된 수신기 의존 실패 | 보고된 `BUFFERING`·`ERROR`를 보존. 완료가 아니므로 대기열 항목을 건너뛰거나 소유 세션의 `FINISHED` 조건을 약화하지 말 것 |
| AirPlay 인증이 실패 | 송신 경로가 다르거나 재생 중 | 지원되는 Linux Server 전용: 재생을 정지하고 표시된 PIN·암호 절차를 다시 진행하며 패키지의 `/usr/local/bin/jastreamer-airplay`와 FFmpeg 경로를 유지. 네이티브 Windows는 다른 경로로도 AirPlay를 켤 수 없음 |
| 화면을 끈 뒤 Android 재생이 멈추거나 로컬 출력이 오프라인이 됨 | 운영체제가 앱을 절전·종료했거나 등록 기한이 만료 | 가능하면 재연결 전에 발생 시각과 `<data_dir>/logs/server.log*`를 보존. Server는 등록·기한 만료, 마지막 조회·갱신·보고 시각, 명령 결과, 미디어 접근 실패를 기록하지만 이는 Server가 관찰한 내용이며 Android가 앱을 중단한 원인은 아님 |
| Android에 재생 오류가 표시되지만 출력은 계속 사용 가능 | 등록 상실이 아니라 미디어·디코더 오류 | `<data_dir>/logs/server.log*`의 `native_playback_error` 확인. 대응하는 Android·Server 빌드는 보고된 예외를 출력·재생·명령 ID와 함께 보관 |

### 재생 오류를 알리는 방식

- 재생 기기 상태 조회가 한 번 실패한 것만으로는 재생을 다시 시작하거나 팝업을 띄우지 않습니다. 세 번 연속 실패하면 상태 경고를 표시하고 조회가 성공하면 자동으로 닫습니다. 경고를 직접 닫으면 같은 실패가 이어지는 동안 다시 띄우지 않습니다. 재생 명령 실패와 확인된 연결 끊김은 즉시 알립니다.
- 이전 연결에서 남은 재생 오류는 접속마다 ‘요청 실패’ 팝업을 다시 띄우지 않고 플레이어의 오류 상세에 남습니다. 새로 보낸 요청의 실패는 계속 팝업으로 알립니다.
- 재생 시작 오류는 실패 단계(`LoadTrack`, `PrepareMedia`, `SetURI`, `Play`)와 제공 가능한 안전한 오류 코드를 함께 표시합니다. UPnP 거부는 액션 이름과 숫자 오류 코드를, Cast는 액션·player state·idle reason·오류 문구를 그대로 보존해 전달하세요. 오류를 지우려고 대기열을 초기화하거나 방화벽을 끄지 마세요. 시간 초과나 전송 실패는 명령 결과를 알 수 없는 상태로 남습니다.
- **Server 모드 로컬 오디오**(브라우저, 네이티브 Windows, 네이티브 Android)에서 재생 중 복구할 수 없는 곡·디코더 오류가 나면 해당 항목을 **재생 실패**로 표시하고 Server가 대기열 순서대로 다음 항목을 시작합니다. 정상 완료로 처리하지 않으므로 실패 항목·중복·순서가 보존되고 실패한 항목을 직접 다시 재생할 수 있습니다. 이어서 재생할 때 조작을 막는 오류 창을 띄우지 않으며 다음 항목이 없으면 오류와 함께 정지합니다. 일시 정지·정지 상태는 재개하지 않고, 네트워크·인증·권한·등록 만료·출력 기기·분류되지 않은 오류로 곡을 건너뛰지 않습니다. 복구 가능한 손상 프레임은 디코더에 맡기며 임의로 앞으로 건너뛰지 않습니다. 네이티브 동작에는 대응하는 Server와 클라이언트가 필요하므로 [업데이트 절차](INSTALL.ko.md#upgrade)에 따라 Server부터 업데이트하세요. 저장된 음악은 로컬 파일용 제한된 실패 처리를 사용하며 로컬 파일 오류를 Server 원본 오류로 보고하지 않습니다.
