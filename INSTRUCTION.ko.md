# jastreamer 0.2 사용자 안내서

[English user guide](INSTRUCTION.md) · [설치 및 업데이트](INSTALL.ko.md) · [프로젝트 소개](README.ko.md)

jastreamer는 Linux 또는 네이티브 Windows에서 Server를 실행합니다. 두 대상 모두 내장 Web 화면과 UPnP/DLNA 출력을 제공하며 Google Cast를 선택적으로 켤 수 있습니다. AirPlay 전송은 Linux 컨테이너 패키지에서만 사용할 수 있습니다. 선택 사항인 Desktop·네이티브 모바일·PWA는 Server에 접속할 수 있습니다. Server 모드의 **이 기기**는 공유 Server 대기열에 로컬 출력을 추가하며 브라우저·Desktop에서는 브라우저 오디오, Android에서는 네이티브 Media3 서비스를 사용합니다. Android **저장된 음악**은 Server 접속이 필요 없는 별도 로컬 보관함과 기기 대기열입니다. 네이티브 iOS 앱은 Server 제어용이며 미디어 로드를 차단합니다. 요구 사항, 패키지 검증, 설치와 업데이트·롤백은 [한국어 설치 및 업데이트 안내서](INSTALL.ko.md)를 따르세요.

## 1. 최초 설정과 일상 사용

1. 설치한 Server의 완전한 사설 LAN 주소를 엽니다. 기본 Linux 리스너는 `http://<server-LAN-IP>:8080/`, 기본 네이티브 Windows 리스너는 같은 사설 LAN 주소의 18080 포트를 사용합니다. 신규 설치에서만 최초 관리자 계정을 만들며 비밀번호는 10자 이상이어야 합니다. 업데이트 후에는 기존 계정·세션을 사용하고 최초 설정을 반복하거나 데이터를 초기화하지 마세요.
2. 기본 언어는 영어입니다. **Settings → 일반**의 **Language / 언어**에서 **English** 또는 **한국어**를 선택하세요. 메뉴 이름은 두 언어 모두 **Settings**로 유지합니다. 언어 선택은 즉시 적용·저장되며 서버 설정 저장이나 재생 명령을 보내지 않습니다.
3. **Settings → 보관함**에서 음악 폴더 경로를 확인해 저장하고 **지금 스캔**을 선택하세요. 표준 Linux 컨테이너의 보관함 경로는 `/music`입니다. 샘플을 포함한 새 설치는 `jastreamer-samples` 아래 MP3 세 개를 두며 이 명시적 스캔 뒤에만 보관함에 나타납니다. FLAC, MP3, WAV/WAVE, Ogg/Vorbis, Opus, M4A를 원본 수정 없이 스캔하며 샘플을 자동으로 대기열에 넣거나 재생하지 않습니다.
   해당 호스트 음악 폴더가 비어 있으면 재생할 곡이 없습니다. 설치 때 확인한 실제 호스트 경로에 음악 파일을 넣고 다시 스캔하세요. 번들 샘플이 있다면 테스트용 곡일 뿐 개인 음악 보관함을 대신하지 않습니다.
   **보관함 스캔**은 보관함 설정 탭의 **음악 폴더** 바로 아래에 있으며 최근 5건의 스캔과 기기 현지 시각 기준 시작·완료 날짜 및 시간을 표시합니다. 표시 제한 때문에 이전 기록을 삭제하지 않습니다. 폴더 변경 저장과 스캔 시작은 별도 동작입니다.
4. Google Cast를 사용하려면 **Settings → 재생·출력**에서 **Google Cast 출력**을 켜고 저장한 뒤 Server를 재시작하세요. `cast.enabled`가 `false`이거나 이전 설정에 없으면 계속 꺼진 상태입니다. 수신기가 보인다는 이유만으로 임의로 켜지 마세요.
5. 보관함을 탐색·검색해 곡이나 묶음 화면을 대기열에 추가하고, 재생이 정지된 상태에서 출력을 선택합니다. 방금 켠 수신기가 없으면 출력 목록을 새로 고치세요.
6. 수신기가 지원할 때 하단 플레이 바에서 재생/일시 정지, 정지, 이전, 다음과 탐색을 사용합니다. AirPlay가 PIN·암호를 요구하면 정지 상태에서 페어링합니다.

### 설정 카테고리

Server의 Settings 화면은 기존 항목을 다섯 탭으로 나눕니다.

| 탭 | 항목 |
|---|---|
| 일반 | 언어, 앱 설치, 서버 이름, 데이터 디렉터리, 계정 비밀번호 |
| 네트워크 | HTTP/HTTPS, 접근 규칙, 네트워크 어댑터, 검색·조회 주기, Server 음원 주소 |
| 보관함 | 음악 폴더, 스캔, 백그라운드 오디오 검증 |
| 재생·출력 | Google Cast, AirPlay·도움말, FFmpeg, 음원 변환 |
| 진단 | 재생·파일 검사 기록과 CSV 보고서 다운로드 |

탭을 바꿔도 저장하지 않은 편집 값이 유지되며 저장·재시작·스캔·재생 명령을 보내지 않습니다. **설정 저장**과 **변경 취소**는 모든 서버 설정 탭의 편집 내용에 함께 적용되며 언어·계정 동작은 별도입니다. 숨은 탭에 유효하지 않은 입력이 있으면 저장 전에 해당 탭과 입력으로 이동합니다. 재시작·설정 충돌 안내는 모든 탭에서 보입니다. 좁은 화면에서는 탭 목록을 좌우로 스크롤하고, 키보드에서는 좌우 화살표·Home·End로 이동할 수 있습니다.

### 재생 및 파일 검사 이력

**Settings → 진단 → 재생 및 파일 검사 기록**에서 Server에 저장되는 공용 이력을 확인하세요. 결과 종류나 렌더러로 필터링할 수 있습니다. 렌더러 보고에는 렌더러 이름·ID, 곡, 오류 코드, 제공된 재생 위치와 펼쳐 볼 수 있는 구조화 진단이 표시됩니다. 이전 로그에서 가져온 기록은 이름 없이 렌더러 ID만 표시될 수 있습니다. 최근 5,000건을 보관하며 이력을 보는 동작은 재생을 변경하지 않습니다.

**지금 스캔**은 증분 방식입니다. 처음에는 모든 음악을 읽고, 이후에는 폴더 목록에서 추가·누락 파일을 확인한 뒤 루트·경로·크기·수정 시각이 같으면 저장된 메타데이터와 완료된 검증 결과를 재사용합니다. 신규·변경 파일을 분석하고 끝나지 않은 검사는 이어서 진행합니다. 변경 없는 파일의 실패·판정 불가 결과도 정상으로 바꾸지 않고 유지하며, Server를 재시작해도 기록이 남습니다.

변경되지 않은 파일까지 메타데이터와 오디오 검증을 모두 다시 수행하려면 별도의 **전체 다시 스캔** 버튼을 누르고 확인하세요. 파일 크기·수정 시각을 그대로 유지한 채 파일을 교체했거나 완료된 검사를 명시적으로 반복하려는 경우에 사용합니다. 곡 ID, 좋아요, 재생 횟수, 플레이리스트와 대기열은 초기화하지 않습니다.

색인이 성공하면 **백그라운드 오디오 검증**이 필요한 파일을 Server의 FFmpeg로 한 번에 하나씩 전체 디코딩하며, 재생 중이거나 다른 스캔이 진행 중이면 일시 중지합니다. 결과 집계에는 기존 결과도 포함됩니다. FLAC은 디코딩된 샘플 수와 STREAMINFO 체크섬이 있으면 그 값도 비교합니다. 색인 완료는 무결성 검사 통과를 뜻하지 않습니다.

검사 실패와 검증할 수 없는 파일은 같은 이력에 음악 폴더 이름·상대 경로로 표시됩니다. 검사 엔진 부재, 지원하지 않는 형식, 시간 초과, 변경되거나 읽을 수 없는 파일을 정상으로 보고하지 않습니다. Server 재시작 후 진행을 이어갑니다. 검사는 원본을 복구·덮어쓰기·삭제하지 않으며 보관함이나 대기열에서 자동 제거하지 않습니다.

PC 또는 휴대전화 웹 브라우저에서 결과 유형을 **파일 검사**로 선택하면 오디오 검증 기록만 추릴 수 있습니다. **CSV 다운로드**를 누르면 현재 페이지뿐 아니라 선택한 필터와 일치하는 보관 기록 전체가 파일로 저장됩니다. UTC 시각, 결과, 곡·음악 폴더·상대 경로, 오류 코드·메시지와 구조화된 상세 정보를 포함하며 재생 기기 보고에는 기기·재생 식별자도 포함합니다. UTF-8 BOM으로 스프레드시트의 한글 인식을 돕고, 수식처럼 시작하는 값은 텍스트로 처리해 실행을 방지합니다.

기존 실패·판정 불가 이력을 내보내는 기능이며 모든 파일의 정상 판정 증명서가 아닙니다. 내보내도 기록을 삭제하거나 원본 파일·재생 상태를 변경하지 않습니다. Android·iOS·Desktop 내장 앱의 기존 다운로드 제한은 유지하고 일반 브라우저에서 같은 Server를 열도록 안내합니다. 필요하면 해당 브라우저에서 별도로 로그인하세요.

### 대기열에서 곡 삭제하기

- 재생 중이거나 일시 정지·선택된 곡도 대기열에서 삭제할 수 있습니다. 하단 플레이어의 현재 곡과 재생 위치는 대기열과 별도로 유지되며, 항목을 삭제해도 정지 명령을 보내거나 오디오를 다시 시작하지 않습니다.
- 대기열 제목 옆의 **대기열 모두 삭제**를 누르고 확인하면 이전·현재·다음 곡을 포함한 모든 항목을 삭제합니다. **좋아요 섞어 담기**와는 별도 작업이며, 원본 음악 파일·좋아요·저장된 플레이리스트는 삭제하지 않습니다.
- 재생 중인 곡은 끝나거나 사용자가 재생 명령을 내릴 때까지 계속 재생됩니다. 대기열이 비어 있어도 일시 정지·재개와 지원되는 위치 탐색을 사용할 수 있습니다. 오디오를 멈추려면 **정지**를 누르세요.
- **한 곡 반복**은 대기열에서 삭제되었거나 대기열 전체가 비어 있어도 현재 곡이 자연스럽게 끝나면 다시 재생하며, 삭제한 항목을 대기열에 복구하지 않습니다. **다음 곡**은 반복 설정과 관계없이 남은 순차·랜덤 순서를 따릅니다. **반복 없음**이나 **전체 반복**에서는 남은 대기열이 없으면 곡 종료 후 정지합니다. 전체 삭제 후 곡을 추가하거나 **다음 순서에 추가**해도 자동 재생하지 않습니다.
- Server를 재시작해도 현재 곡 선택과 저장 위치는 유지하되 삭제한 대기열을 복구하거나 자동 재생하지 않습니다. Android **저장된 음악**의 별도 로컬 대기열 동작은 바꾸지 않습니다.

### 폴더 탐색과 음악 목록 작업

**보관함 → 폴더** 안의 **상위 폴더**는 같은 음악 루트에서 한 단계 위로 이동합니다. 음악 루트에서는 **목록으로**가 음악 루트 목록으로 돌아가며, **폴더** 탭으로도 바로 그 목록에 갈 수 있습니다. 폴더 이동은 대기열이나 재생을 바꾸지 않습니다.

폴더의 **전체 재생**, **다음 순서에 추가**, **대기열 끝에 추가**, **저장된 플레이리스트에 추가**는 현재 폴더와 모든 하위 폴더에서 검색 조건에 맞는 재생 가능한 곡을 경로 순서로 처리합니다. 중간 폴더에 곡이 없거나 목록이 여러 페이지여도 하위 곡을 모두 포함하며, 다른 음악 루트나 형제 폴더는 포함하지 않습니다. 폴더 화면 자체는 계속 한 단계씩 탐색합니다.

- **전체 재생**: 기존 재생을 정지하고 대기열을 선택한 곡들로 바꾼 뒤 재생합니다.
- **다음 순서에 추가**: 현재 곡 바로 뒤에 넣습니다. 현재 곡이 없으면 대기열 맨 앞에 넣습니다.
- **대기열 끝에 추가**: 기존 대기 곡들 뒤에 붙입니다. 위의 추가 동작 두 가지 모두 현재 재생을 끊거나 자동으로 시작하지 않습니다.
- **저장된 플레이리스트에 추가**: 기존 저장 목록에 붙이거나 새 목록을 만듭니다. 재생 대기열에는 영향을 주지 않습니다.

탐색과 목록 작업 버튼은 한 줄의 도구 모음에 모았습니다. 좁은 화면에서는 도구 모음을 좌우로 스크롤하세요. **대기열 끝에 추가**는 목록·아래 화살표 아이콘으로 현재 재생 대기열에 넣습니다. **저장된 플레이리스트에 추가**는 북마크·더하기 아이콘으로 저장 목록 선택 창을 열며, 저장만으로 재생을 시작하지 않습니다.

<a id="likes"></a>
### 좋아요와 일회성 대기열 섞기

- 보관함, 플레이리스트, 대기열, 곡 정보의 하트 버튼으로 좋아요를 설정·해제하세요. 네 화면 모두 좋아요를 선택하면 채워진 하트, 해제하면 윤곽 하트로 표시하며, 대기열의 중복 곡과 열려 있는 곡 정보 창도 함께 갱신됩니다. 좋아요를 바꿔도 대기열 순서와 재생 상태는 변하지 않습니다. 좋아요는 계정별 비공개 목록이 아닌 Server 공용 상태이며 다시 스캔하거나 Server를 재시작해도 유지됩니다.
- 보관함의 **좋아요**를 선택하면 좋아요 곡을 탐색할 수 있습니다. 검색과 페이지 구분은 이 화면에도 적용됩니다.
- **대기열 → 좋아요 섞어 담기**를 누르면 현재 페이지나 검색 결과와 관계없이 재생 가능한 좋아요 곡 전체를 무작위 순서로 대기열 끝에 추가합니다. 기존 대기열의 순서와 중복 곡을 유지하고 현재 재생을 끊지 않으며, 정지 상태에서 자동 재생하지 않습니다. 사용할 수 없는 곡은 제외합니다. 대상이 없거나 좋아요 곡이 10,000곡을 넘거나 추가 후 전체 대기열이 10,000곡을 넘으면 일부만 추가하지 않고 오류를 표시합니다.
- 공용 대기열에 일회성으로 담는 기능이며 저장된 플레이리스트나 실시간 좋아요 필터가 아닙니다. 이후 좋아요를 바꿔도 담아 둔 곡은 바뀌지 않습니다. 대기열은 기존처럼 Server 재시작 후에도 보존되며 자동 재생하지 않습니다.
- 이름을 입력할 필요가 없고 저장된 플레이리스트를 만들지 않습니다. 결과를 보관하고 싶을 때만 대기열의 일반 **플레이리스트로 저장** 기능을 사용하세요. 이전에 만든 좋아요 셔플 목록을 포함한 기존 플레이리스트는 그대로 유지됩니다.

<a id="playback-modes"></a>
### 재생 순서와 반복

- 플레이 바에서 **순차 재생 / 랜덤 재생**과 **반복 없음 / 전체 반복 / 한 곡 반복**을 각각 선택합니다. 데스크톱에서는 재생 버튼 옆에 있고, 휴대전화에서는 미니 플레이어의 **재생 모드** 버튼이나 펼친 재생 제어에서 바로 조작할 수 있습니다.
- **순차 재생**은 표시된 대기열 순서대로 진행합니다. **랜덤 재생**은 표시 순서를 바꾸거나 같은 곡의 중복 항목을 합치지 않고 별도의 무작위 순서로 진행합니다. 한 번의 순회에서는 각 대기열 항목을 한 번씩 선택하며, 랜덤을 켜거나 대기열을 교체하면 새 순회를 만듭니다. 명시적인 **다음 순서에 추가**는 이후 다른 곡을 대기열 끝에 추가해도 우선합니다. 일반 추가 묶음은 내부 순서를 섞어 현재 순회의 끝에 붙습니다.
- **반복 없음**은 마지막에 정지하고, **전체 반복**은 다시 순회하며 랜덤 모드에서는 새 순서를 만듭니다. **한 곡 반복**은 곡이 자연스럽게 끝났을 때만 적용하므로 **다음 곡**을 누르면 다른 곡으로 이동합니다. 미디어 오류를 무한 반복하지 않습니다.
- 랜덤 모드의 **이전 곡**은 다시 무작위로 고르지 않고 현재 순회에서 지나온 순서를 따릅니다. 탐색 가능한 곡을 5초 넘게 재생했다면 기존처럼 현재 곡의 처음으로 돌아갑니다.
- 모드를 바꿔도 정지 상태에서 재생을 시작하거나 현재 곡을 다시 시작하지 않으며 재생 위치·저장된 플레이리스트를 유지합니다. Server 모드는 공용 대기열과 모든 출력에 적용되고 다른 Control에도 동기화되며 재시작 뒤에도 보존하되 자동 재생하지 않습니다. Android 저장된 음악은 별도의 로컬 모드를 사용합니다.

<a id="most-played"></a>
### 재생 횟수와 많이 재생한 곡

- **보관함 → 많이 재생한 곡(Most Played)**에서 재생 횟수가 한 번 이상 집계된 재생 가능한 곡을 횟수 내림차순으로 봅니다. 각 곡에 **N회**를 표시하며, 전체 결과를 정렬한 뒤 검색 조건과 페이지 구분을 적용합니다. 곡 정보에도 같은 횟수를 표시하고 아직 집계되지 않은 곡은 **0회**로 표시합니다.
- 계정별 비공개 통계가 아닌 Server 전체 공용 통계입니다. 한 재생 세션에서 재생기로부터 확인한 재생 시간이 **30초**, 1분 미만 곡은 **곡 길이의 절반**에 도달하면 1회입니다. 길이를 모르는 곡은 30초를 기준으로 합니다. 연결된 재생기의 재생 상태와 위치 진행을 이용하며, 실제 스피커 소리를 사람이 들었다는 증거는 아닙니다.
- 일시 정지 후 이어서 재생하면 해당 세션의 재생 시간을 이어 계산하며 같은 세션을 중복 집계하지 않습니다. 일시 정지 시간, 탐색으로 건너뛴 시간, 재생 위치가 진행하지 않은 시간과 재생 상태를 확인할 수 없는 공백은 제외합니다. 새로 다시 재생한 곡은 기준에 도달하면 추가로 집계합니다.
- 기능 적용 이후부터 집계하며 과거 횟수를 추정해 채우지 않습니다. 확정된 횟수는 다시 스캔하거나 Server를 재시작해도 유지됩니다. 아직 기준에 도달하지 않은 재생 시간은 Server 재시작 뒤 복원하지 않습니다. Server와 무관한 Android **저장된 음악** 재생은 포함하지 않습니다.
- 통계를 탐색해도 대기열을 바꾸거나 재생하지 않습니다. 횟수는 음악 파일 태그가 아닌 Server 데이터베이스에 저장하며 원본 음원을 변경하지 않습니다.

<a id="browser-output"></a>
### 이 기기: 로컬 오디오 출력

아래 브라우저 안내는 브라우저·PWA·Desktop에 적용되며 네이티브 iOS 앱에는 적용되지 않습니다. 네이티브 Android 앱은 [Android 로컬 재생](#android-controls)을 참고하세요. 브라우저와 네이티브 Android 출력은 **로컬 오디오**로 표시되며 Server 대기열을 사용합니다.

1. 재생을 정지하고 **재생 기기**에서 **(이 기기)**가 붙은 출력(예: **Windows · Chrome (이 기기) [로컬 오디오]**)을 선택하세요. 휴대전화에서는 간결한 플레이어를 펼치면 선택기가 나타납니다.
2. 곡을 고르거나 기존 대기열에서 **재생**을 누르세요. 브라우저가 오디오를 차단하면 해당 페이지의 **재생 허용**을 직접 누릅니다. 대기 중이던 요청이 이미 실패했다면 오류를 닫고 다시 재생하세요. 명령을 조용히 재전송하지 않습니다.
3. 일시 정지·정지·이전·다음과 지원되는 탐색을 사용하세요. 별도 로컬 대기열을 만드는 것이 아니라 기존 Server 대기열의 출력으로 동작합니다.

페이지에 들어올 때 기존 선택 출력이 오프라인이고 재생이 정지·사용 불가 상태이면 **이 기기**를 한 번 자동 선택합니다. 대기열·현재 곡·저장된 위치는 유지하며 **재생**은 직접 눌러야 합니다. 다른 화면에서 출력이나 재생 상태가 바뀌었거나 기존 출력이 다시 온라인이 되면 전환하지 않습니다. Android에서는 저장된 음악이나 다른 Server의 재생 소유권을 가져오지 않습니다. 이미 열린 페이지에서 연결이 끊겼다고 계속 재등록하지는 않습니다.

현재 페이지·앱이 소유한 **이 기기**가 선택되면 **로컬 음량**으로 0–100%를 조절할 수 있습니다. 휴대전화에서는 플레이어를 펼치세요. 브라우저 오디오 또는 Android의 Server 모드 Media3 음량만 바꾸며 운영체제 전체 음량·네트워크 렌더러·다른 기기의 음량은 바꾸지 않습니다. Android에는 대응하는 APK가 필요하며 구버전 앱에서는 이 제어가 표시되지 않습니다.

기본 이름은 페이지에서 확인할 수 있는 OS·브라우저 정보이며, 컴퓨터의 호스트 이름이나 사용자가 휴대전화에 지정한 이름을 읽은 값은 아닙니다. 출력 선택기 옆 연필 버튼 **로컬 오디오 출력 이름 변경**에서 **작업실 PC** 같은 별칭을 저장하세요. **기본 이름 사용**을 누르거나 별칭을 비워 저장하면 자동 이름으로 돌아갑니다. 이름은 UTF-8 기준 80바이트까지이며 한글은 글자당 여러 바이트를 사용합니다.

별칭은 이 브라우저 프로필에서 해당 Server UUID와 정확한 origin(포트 포함)별로 저장합니다. 다른 브라우저·프로필, 비공개 탐색 또는 교체된 Server로 자동 이전하지 않습니다. 저장소 오류가 나면 저장됐다고 표시하지 않고 오류를 알리며, 브라우저 저장소를 지우면 별칭도 없어집니다.

해당 출력을 등록·소유한 페이지에서만 **작업실 PC (이 기기)**처럼 표시합니다. 같은 계정이어도 다른 페이지·기기에서는 **작업실 PC**만 보입니다. 아직 출력을 등록하지 않은 페이지에도 자기 로컬 출력을 선택할 수 있는 **(이 기기)** 항목이 있으며, 다른 렌더러에 이 표시를 붙이는 것은 아닙니다. 등록 전 이름 저장만으로 출력을 등록·선택하지 않습니다. 등록된 출력의 이름을 바꾸면 다른 화면에도 갱신되지만 출력 ID·대기열·재생·출력 선택은 바뀌지 않습니다. 별칭은 표시 이름이지 검증된 하드웨어 식별자가 아닙니다.

제어는 동일 origin의 인증된 HTTP JSON, 음원은 브라우저 오디오 요소의 HTTP(S) GET/Range를 사용합니다. UPnP·HLS·DASH·WebRTC가 아닙니다. 실제 스피커·헤드폰은 브라우저와 운영체제가 선택하며, 하드웨어 출력 선택기·네이티브 WASAPI/ASIO 엔진·독점 모드·bit-perfect 보장은 없습니다. 지원 형식은 브라우저 디코더에 따라 다릅니다. 변환에는 변환 활성화와 설정된 FFmpeg가 필요하며 변환된 WAV 스트림은 탐색할 수 없습니다.

재생을 담당하는 브라우저 페이지를 열어 두세요. 해당 페이지를 닫거나 새로고침하면 브라우저 출력이 해제되며, 등록의 연결 유지가 끊기면 대기열을 버리지 않고 출력을 사용 불가로 처리합니다. 다시 들어오면 위 조건에서 자동 선택하며, 조건을 충족하지 않으면 필요에 따라 정지한 뒤 **이 기기**를 다시 선택하고 직접 재생하세요. 다른 Control을 닫아도 재생 담당 브라우저는 정지하지 않으며 네트워크 출력은 Control 종료와 무관하게 동작합니다. Windows Desktop의 X는 아래 설명처럼 페이지를 닫지 않고 숨깁니다. 일반 브라우저·PWA의 백그라운드·잠금 화면 재생은 브라우저와 운영체제에 따라 달라 보장하지 않습니다. Desktop도 컴퓨터 절전·로그아웃 뒤 재생을 보장하지 않으며, 네이티브 Android 앱은 아래 서비스를 사용합니다.

<a id="phone-controls"></a>
### 휴대전화 화면과 설치형 Web 앱

휴대전화 화면은 iPhone 또는 사용자 에이전트에 Android와 Mobile이 함께 표시되는 휴대전화에서만 자동으로 사용됩니다. iPad와 Android 태블릿을 포함한 태블릿은 창 너비와 관계없이 기존 표준 화면을 유지합니다.

휴대전화에서는 위쪽 헤더에서 현재 사용자와 로그아웃을 확인하고, 아래쪽의 **보관함**, **플레이리스트**, **대기열**, **Settings** 네 탭으로 이동합니다. 일반 브라우저에서는 jastreamer 로고를 그대로 표시하며 Android·Desktop·iOS 앱 안에서만 중복되는 Web 헤더·사이드바 로고를 숨깁니다. 계정 표시와 로그아웃은 유지됩니다. 메뉴 이름 **Settings**는 한국어와 영어에서 모두 같습니다. 주요 재생·탐색·곡 동작 버튼은 최소 44×44px 터치 영역을 제공하고, 헤더·재생 바·하단 탐색은 기기의 safe area 간격을 반영합니다.

아래쪽의 간결한 플레이어에는 현재 곡, **재생 모드**, **재생/일시 정지**, **정지**와 펼치기 화살표가 있습니다. 재생 모드 버튼으로 순차·랜덤과 반복을 바로 바꿀 수 있습니다. 화살표를 누르면 이전·다음 곡, 순차·랜덤·반복, 탐색, 출력 선택·새로 고침과 필요한 AirPlay 페어링 제어가 표시됩니다. 지원되지 않는 수신기 기능은 비활성화됩니다. 앨범 아트를 누르면 **대기열**로 이동합니다.

다른 하단 탭으로 이동하면 재생을 정지하지 않고 펼친 플레이어만 접힙니다. 키보드 포커스가 플레이어 안에 있을 때 Escape로도 접을 수 있습니다. 높이가 낮거나 가로 방향인 화면에서는 펼친 패널 안을 스크롤할 수 있으며, 패널이 덮고 있는 본문은 접기 전까지 조작되지 않습니다.

선택 사항인 설치형 Web 앱(PWA)은 같은 화면을 홈 화면 바로가기에서 열며 Server의 대기열과 재생을 그대로 제어합니다. 항상 Server에 연결할 네트워크가 필요하고 service worker·cache·오프라인 재생은 제공하지 않습니다. **Settings**의 바로가기 설치 항목은 제거했지만 기존 바로가기는 계속 사용할 수 있고 지원 브라우저의 자체 메뉴로 추가할 수 있습니다. 안전한 HTTPS 준비와 브라우저별 절차는 [휴대전화 Web 앱 설치](INSTALL.ko.md#pwa)를 참고하세요. 선택 사항인 Desktop 앱에는 PWA 설치가 필요하지 않습니다.

대기열은 Server 전체에서 공유되며 순서와 중복을 보존하고 재시작 뒤에도 남습니다. Server 재시작 뒤 재생은 자동으로 이어지지 않습니다. Cast도 이 대기열 하나를 사용하고 Cast autoplay를 끈 상태로 미디어를 LOAD한 뒤 Play를 명시적으로 전송합니다. 수신기 그룹과 gapless 재생은 지원하지 않습니다.

### Desktop 진입 화면

Desktop 앱은 **서버 재생** 화면에서 **지금 발견한 서버**와 **최근 연결한 서버**를 별도 카드 영역으로 보여줍니다. 카드에는 서버 이름·주소·확인된 연결 상태가 표시되며, 저장된 기록만으로 현재 연결 가능하다고 판단하지 않습니다. 카드의 연결 버튼을 누르거나 **주소로 직접 연결**을 눌러 주소 입력창을 여세요. 취소나 Escape는 연결하지 않고 창을 닫으며 다시 열면 입력 초안이 유지됩니다. 검색·화면 이동·연결만으로 재생하지 않습니다.

Desktop에는 독립 저장 음악 플레이어나 로컬 재생 홈 카드가 없습니다. 연결 후 공유 Web 화면의 **이 기기** 출력은 기존 Server 보관함과 대기열을 사용합니다.

Windows에서는 창의 **X**를 눌러도 트레이에 상주하며 연결과 로컬 재생이 계속됩니다. 트레이 아이콘 클릭·더블 클릭 또는 우클릭 메뉴의 **JASTREAMER 열기**로 같은 창을 복원합니다. 앱을 다시 실행해도 기존 창을 엽니다. 완전히 끝내려면 트레이 우클릭 → **종료**를 선택하세요. 이때 해당 앱의 로컬 출력은 해제되지만 다른 네트워크 출력에 정지 명령을 보내지는 않습니다. 업데이트 전에 이 방법으로 종료하세요. 트레이를 만들 수 없는 환경에서는 X가 기존처럼 앱을 종료하며, Linux의 창 닫기 동작은 바뀌지 않습니다.

<a id="android-controls"></a>
### 네이티브 Android 화면

Kotlin 앱은 공유 Web 화면 바깥에 서버 선택, Server가 제어하는 휴대전화 출력과 독립 **저장된 음악** 플레이어를 제공합니다. [호환되는 Server/Web 버전, 설치와 APK 업데이트 규칙](INSTALL.ko.md#android)은 PWA 설치와 별도입니다.

- 앱의 홈에는 **서버 재생**과 **로컬 재생** 카드가 있습니다. **로컬 재생**은 연결이나 로그인 없이 기존 **저장된 음악**을 열며, 카드의 보조 **다운로드** 동작은 기기의 전송 관리자를 엽니다. **서버 재생**을 누르면 **지금 발견한 서버**와 **최근 연결한 서버**가 구분됩니다. 주소를 직접 입력하려면 **주소로 직접 연결**을 누른 뒤 HTTP(S) 루트 주소를 입력하고 **확인 후 연결**을 선택하세요. 새 실행에서는 자동 연결하지 않으며 Server 식별자 검증은 그대로 유지합니다.
- Server 모드의 네이티브 헤더에는 선택한 Server와 포트를 포함한 완전한 origin이 표시됩니다. **서버**는 Server가 제어하는 네트워크 출력이나 저장된 음악 재생을 정지하지 않고 선택 화면으로 돌아갑니다. 저장 항목의 Server UUID가 달라지면 기존 세션을 조용히 재사용하지 않고 거부합니다.
- Android의 시스템 바·키보드 여백은 네이티브 셸에서 한 번만 적용합니다. 뒤로 가기는 직접 연결 창이나 키보드를 먼저 닫고 현재 Web 또는 저장 음악의 내부 탐색을 따릅니다. Web 기록 끝에서는 서버 선택으로, 서버 선택과 저장 음악의 최상위 화면에서는 홈 카드로 돌아가며 홈에서 다시 누르면 앱을 나갑니다. 회전은 현재 화면과 저장하지 않은 Web 입력을 유지합니다. 백그라운드에서 돌아오면 Server 페이지를 보이기 전에 식별자를 다시 확인하지만 확인 실패가 **저장된 음악**을 막지는 않습니다.
- Cookie와 Web 저장소는 Server UUID와 포트를 포함한 완전한 origin별로 격리됩니다. **최근 서버 목록에서 제거**는 바로가기만 지우며 로그아웃, 격리 프로필 삭제 또는 저장된 음악 삭제가 아닙니다. 세션을 끝내려면 Server 화면에서 로그아웃하세요. 로그아웃하면 해당 Server의 미완료 가져오기는 중지되지만 이미 확정한 음악은 기기 소유로 남습니다.
- 네이티브 언어 선택과 **Settings → 일반 → Language / 언어**에서 영어·한국어를 지원합니다. Web에서 바꾼 언어는 페이지 로딩이 끝나거나 화면을 나가거나 일시 중단할 때 네이티브 화면에 반영됩니다.

**저장된 음악과 다운로드**

- **저장된 음악**은 Server HTML·네트워크·로그인 없이 앱의 **보관함 / 플레이리스트 / 대기열 / Settings**를 엽니다. 보관함의 기본 화면은 **앨범**이며 **아티스트 / 장르 / 폴더 / 곡 / 좋아요**와 검색·상세 탐색을 제공합니다. 장르는 저장된 메타데이터를 사용하며 좋아요는 Server가 아닌 이 기기에 속합니다. 완료된 가져오기는 로컬 메타데이터와 앨범 아트를 포함하며 로그아웃, 세션 만료, 계정 상실, Server/프로필 제거 또는 구형·접속 불가 Server와 관계없이 사용할 수 있습니다.
- 곡과 모음은 Server와 같은 **재생 / 다음에 재생 / 맨 뒤에 추가 / 저장 목록에 추가 / 곡 정보** 흐름을 사용합니다. 선택은 현재 분류와 검색 결과를 따릅니다. 네이티브 이동·삭제는 기기 소유 저장소만 관리합니다.
- 호환 Server/Web UI와 Android 앱은 곡·앨범·플레이리스트·폴더에 별도 **다운로드** 버튼을 표시할 수 있습니다. 저장 위치와 **원본**(검증한 원본 바이트) 또는 Server가 지원할 때 **공간 절약(AAC-LC 256 kbps M4A)**을 확인하세요. 다운로드는 재생을 시작하거나 Server 공용 대기열을 바꾸지 않습니다. 플레이리스트 가져오기는 순서와 중복 항목을 보존하는 일회성 스냅샷이며 이후 Server 편집과 동기화하지 않습니다.
- Server의 **폴더** 화면에서 보관함 루트·하위 폴더·현재 열린 폴더를 다운로드할 수 있습니다. 하위 경로를 포함해 등록된 곡 최대 10,000개의 스냅샷을 선택한 로컬 폴더로 가져옵니다. Server의 디렉터리 구조를 복제하거나 플레이리스트를 만들지 않으며, 이후 변경 사항을 반영하려면 직접 다시 가져와야 합니다.
- 요청이 접수되면 Server 화면에서 바로 진행 상태를 엽니다. **다운로드** 항목은 현재 문서에서 시작한 작업의 받은 용량·곡 수·대기 이유·완료/실패를 표시합니다. 진행 중인 다운로드 버튼은 상태를 다시 열고, **다시 다운로드**는 새 확인 절차를 시작합니다. 앱 재실행 후 복원된 작업을 포함한 전체 작업은 네이티브 다운로드 관리자에서 확인하세요.
- 기본 정책은 데이터 제한이 없는 비모바일 연결을 기다립니다. 대기 중인 작업에서 **다운로드 네트워크 설정**을 누르면 Server 화면을 나가지 않고 설정할 수 있습니다. 과금망·모바일 데이터 사용은 네이티브 확인 창에서 직접 승인하거나 관리자의 **데이터 제한 또는 모바일 데이터 허용**을 켜야 하며, 취소만으로 허용되지 않습니다. 로밍·연결 불가는 계속 차단하고, 도달 가능한 로컬 Server를 이용하는 데 인터넷 연결 검증은 요구하지 않습니다.
- 네이티브 **다운로드** 관리자는 준비·전송·검증·로컬 가져오기·일부 실패·일시 정지/계속·취소·목적지 변경을 제공합니다. 미완료 작업은 시작한 Server와 로그인 주체에 묶입니다. v1 기능이 없는 Server에서는 새 가져오기를 시작할 수 없지만 기존 Server 제어는 그대로 사용할 수 있고 이미 저장된 음악에는 영향이 없습니다.
- **다운로드 기록 제거**는 확인 후 완료·일부 저장·취소·실패 기록 하나를 지웁니다. **완료된 기록 지우기**는 해당 기록을 모두 지우되 진행 중이거나 일시 정지된 작업은 유지합니다. 두 동작 모두 저장된 음악·앨범 아트·폴더·플레이리스트·대기열을 남깁니다. 진행 중인 작업은 별도로 취소한 뒤 기록을 제거하세요. 호환 Server/Web 상태 화면에서도 갱신 시 제거한 네이티브 기록이 사라집니다.
- 로컬 플레이리스트를 만들고 이름·순서·항목을 바꾸거나 삭제해도 Server 플레이리스트는 바뀌지 않습니다. 목록 항목 제거 또는 로컬 목록 삭제만으로 오디오 파일을 지우지 않습니다. 로컬 대기열은 순서와 같은 곡의 반복 항목을 보존합니다. 특정 반복 항목부터 재생하거나 항목을 이동·제거하고, 대기열을 로컬 플레이리스트로 저장할 수 있습니다. 항목 추가만으로 재생을 시작하거나 재생 위치를 초기화하지 않습니다. 탐색·이전/다음·셔플·반복도 Server 대기열과 독립적입니다.
- 로컬 미니 플레이어에서도 **순차 재생 / 랜덤 재생**과 반복 버튼을 바로 사용할 수 있으며 정지 상태나 빈 대기열에서도 설정할 수 있습니다. 펼친 플레이어·대기열과 같은 로컬 설정을 표시하고, 자동 재생이나 표시된 대기열 재정렬 없이 바뀝니다. Server의 재생 모드에는 영향을 주지 않습니다.
- 저장된 음악은 Server와 같은 탐색 구조·어두운 배경·녹색 강조 및 접근성 이름이 있는 아이콘 제어를 사용합니다. 미니 플레이어는 계속 표시하며 그 위로 탐색·이전/다음·셔플·반복·대기열·곡 정보 제어를 펼칩니다. 큰 앨범 아트나 재생 막대를 중복 표시하지 않고, 넓은 화면에서는 곡 정보와 제어를 좌우로 배치합니다. 펼치거나 닫아도 탐색 화면·검색어·스크롤 위치·대기열·재생 소유권을 유지합니다. 휴대전화에서는 재생 제어를 펼친 동안 뒤쪽 탐색 영역의 입력을 차단합니다.
- **폴더**는 앱 전용 지속 음악 저장소 안의 실제 디렉터리입니다. 폴더 생성·이름 변경·이동, 선택한 곡 이동, 다운로드 목적지 선택과 덮어쓰기 없는 같은 이름 충돌 처리를 제공합니다. 이동 뒤에도 안정된 로컬 ID·플레이리스트·대기열·재생 위치를 유지합니다. 중단된 저장소 작업은 기록을 바탕으로 복구한 뒤 다음 보관함 변경을 허용합니다. NAS 폴더나 다른 앱 파일을 탐색·변경하는 기능은 아닙니다.
- **기기에서 삭제**는 확인 뒤 선택한 로컬 오디오를 영구 삭제하고 로컬 플레이리스트·대기열 참조를 갱신합니다. 로컬 플레이리스트만 삭제하면 오디오는 남습니다. 현재 곡이 포함되면 **재생 종료 후 삭제** 또는 명시적인 **지금 정지하고 삭제**를 선택하며, 보류 삭제는 재생이 파일을 놓을 때까지 기다립니다.

**재생 소유권**

- 하나의 Media3 서비스가 이 Android를 없음·Server·저장된 음악 중 한 소유자로만 운용합니다. Server 소유 중 로컬 음악을 시작하거나 로컬 소유 중 휴대전화를 Server 출력으로 지정하면 명시적으로 전환을 확인합니다. 이전 소유자를 정지한 뒤 다음 소유자에게 넘기며 대기열을 복사하지 않고 이전 소유자의 늦은 명령도 경계를 넘지 못합니다.
- Server 모드에서는 **Android · jastreamer (이 기기) [로컬 오디오]** 또는 저장한 별칭을 직접 선택한 뒤 **재생**을 누르세요. 이때 대기열과 명령은 Server가 소유하며 Android 시스템 미디어 제어의 재생·일시 정지·정지·이전·다음·지원되는 탐색도 Server로 전달합니다. 저장된 음악 모드에서는 같은 제어가 로컬 파일과 기기 대기열에 실행됩니다.
- 네이티브 셸은 화면 닫기·백그라운드 전환·서버 변경·다시 열기만으로 Play·Stop을 보내지 않습니다. 서비스가 살아 있으면 Activity·WebView 재생성 뒤에도 재생이 유지됩니다. 프로세스 종료나 등록의 최종 상실은 Server 소유 재생을 정지합니다. 로컬 보관함·대기열·위치는 재시작 뒤 복원하지만 자동 재생하지 않습니다. Server 페이지를 새로 열 때만 위의 오프라인 출력 자동 선택을 시도할 수 있으며 저장된 음악의 소유권이나 진행 중인 사용자 요청을 빼앗지 않습니다.
- 지원 형식은 Media3·Android 디코더에 따라 다릅니다. 원본 가져오기는 디코더 호환, 독점 모드나 bit-perfect를 보장하지 않습니다. Server 모드의 일시적 음원 오류는 제한적으로 복구하며 최종 오류는 무한 재시도하지 않고 표시합니다.
- 확인된 Server의 현재 최상위 문서에만 Server 출력과 명시적 가져오기용 제한된 네이티브 인터페이스를 제공합니다. Cookie·자격 증명·임의 네이티브 호출·파일시스템 탐색·임의 URL 다운로드는 노출하지 않습니다. 외부 탐색, 새 창, 파일 선택기, 그 밖의 Web 다운로드와 네이티브 권한 요청은 계속 차단합니다. Desktop과 일반 브라우저는 브라우저 오디오를 유지하며 네이티브 iOS에는 Android 인터페이스나 로컬 재생을 추가하지 않습니다.

공유 Web 화면의 PWA 설치 카드는 브라우저용이며 네이티브 Android 클라이언트에는 PWA 추가 설치가 필요하지 않습니다. 특히 암호화하지 않는 HTTP에서는 신뢰하는 Server만 사용하세요.

<a id="ios-controls"></a>
### 네이티브 iOS 화면

SwiftUI 앱은 같은 Server Web 화면을 감쌉니다. 현재 제공 범위는 설치 가능한 휴대전화 릴리즈가 아닌 [소스와 CI](INSTALL.ko.md#ios)입니다.

- 서버 전용 선택 화면은 **지금 발견한 서버**와 **최근 연결한 서버**를 구분합니다. 주변 서버 카드는 검색 검증을 통과한 항목이며 최근 카드는 연결할 때 다시 확인하고 저장된 기록만으로 온라인이라고 표시하지 않습니다. **주소로 직접 연결**을 누르면 네이티브 입력창이 열리며 HTTP(S) 루트 주소를 입력하고 **확인 후 연결**을 선택할 수 있습니다. 취소해도 입력 초안은 유지되며 검증 중이면 해당 연결 시도를 취소한 뒤 닫습니다. 새로 실행하면 자동 연결하지 않고 선택 화면에 머뭅니다.
- 헤더에는 선택한 Server와 포트를 포함한 완전한 origin이 표시됩니다. **서버 변경**은 네트워크 출력의 재생을 멈추지 않고 서버 선택으로 돌아갑니다. 백그라운드에서 돌아오면 페이지를 보여주기 전에 Server UUID를 다시 확인하며 식별자·네트워크 오류에서는 이전 페이지와 키보드에 접근할 수 없습니다.
- 쿠키와 Web 로컬 저장소는 확인된 Server UUID와 정규화한 scheme·host·port로 구분한 named profile을 사용합니다. 다른 포트는 별도 세션입니다. 최근 항목 제거는 목록만 바꾸며 세션을 끝내려면 Server 화면 안에서 로그아웃하세요.
- 뒤로 가기는 사용할 수 있는 Web 기록으로 이동하고 새로고침은 페이지를 다시 엽니다. 입력에는 키보드의 **다음**과 **완료**를 사용할 수 있습니다. 회전은 실행 중인 페이지와 저장하지 않은 입력을 유지합니다. 높이가 작은 키보드 화면에서는 뒤로·새로고침 바가 숨지만 서버 변경과 언어 선택은 유지됩니다.
- 네이티브 언어 선택과 **Settings → 일반 → Language / 언어**에서 영어·한국어를 지원합니다. JavaScript bridge 없이 페이지 로딩, 백그라운드 복귀와 네이티브 화면 전환 때 Web 언어를 읽어 반영합니다. 네이티브 언어를 바꾸면 Web 페이지를 다시 불러오므로 저장하지 않은 편집을 먼저 마치세요.
- 네이티브 셸은 종료, 백그라운드 전환과 서버 변경에서 Play·Stop을 보내지 않습니다. Web 미디어 로드를 차단하며 로컬 재생 진입점, 독립 네이티브 오디오 엔진, 오프라인 재생이나 명령 cache는 없습니다. 외부 탐색, 새 창, 다운로드, 파일 선택기와 네이티브 미디어·기기 권한 요청도 차단합니다. 브라우저 오디오나 파일 업로드가 필요하면 일반 브라우저를 사용하세요.

공유 PWA 카드는 브라우저용이며 네이티브 앱 안에 추가 설치하지 않습니다. 특히 암호화하지 않는 HTTP에서는 신뢰하는 Server만 사용하세요. 검색이 되지 않으면 로컬 네트워크 접근, Wi-Fi·멀티캐스트와 VPN 경로를 확인하거나 완전한 주소를 직접 입력하세요.

### Google Cast 미디어와 완료 판단 범위

Cast 원본 직접 스트리밍은 검사된 codec·sample rate·channel 메타데이터와 해당되는 경우 bit depth를 보수적으로 판단합니다. 모든 직접 전송 원본은 일치하는 codec이 확인되고 sample rate가 양수이며 mono 또는 stereo여야 합니다. FLAC은 96 kHz 및 1–24-bit까지, MP3·Ogg/Vorbis·Ogg/Opus·M4A/AAC는 48 kHz까지, LPCM WAV는 48 kHz 및 1–16-bit까지 허용합니다. 메타데이터가 없거나 맞지 않는 원본 및 이 제한 밖의 형식은 호환된다고 가정하지 않습니다. 미디어 변환을 켜고 FFmpeg를 설정하면 탐색할 수 없는 44.1 kHz stereo 16-bit WAV 스트림을 대신 사용합니다. 원본 파일은 변경하지 않습니다. 직접 스트리밍은 이 변환을 피할 뿐 수신기의 bit-perfect 출력을 보장하지 않습니다.

Cast 제어는 지속 TLS 연결을 유지하고 자신이 실행한 애플리케이션·미디어 세션을 소유합니다. 대기열은 그 미디어에 대한 명시적인 `FINISHED` 상태를 받아야만 진행합니다. EOF, 빈 상태, `BUFFERING` 또는 `ERROR`를 완료로 처리하지 않습니다. 일시 정지·탐색·허용 형식은 여전히 수신기에 따라 다릅니다.

### 서버 경로와 네트워크 선택

- 음악 폴더, 데이터 디렉터리, HTTPS 인증서·개인 키, FFmpeg와 **AirPlay 송신 프로그램 경로** 옆 **찾아보기**는 브라우저 기기가 아닌 로그인한 **서버의 파일 시스템**을 엽니다. Windows는 접근 가능한 드라이브를 표시하며 절대 UNC 공유 경로를 입력할 수 있고, Linux/NAS는 `/`에서 시작합니다. 컨테이너에서는 마운트된 경로만 보입니다. 목록에서 심볼릭 링크와 Windows 재분석 지점은 제외하며 직접 경로 입력도 유지합니다.
- 루트, 상위 폴더 또는 절대 디렉터리 경로로 이동하세요. **선택**은 편집 중인 필드만 바꾸고 **취소**는 기존 값을 유지합니다. 설정을 명시적으로 저장한 뒤 음악 폴더를 스캔하세요. 데이터 디렉터리를 선택해도 기존 데이터베이스나 앨범 아트가 이동하지 않습니다. 저장 위치를 변경하고 재시작하기 전에 기존 데이터를 별도로 보존하세요.
- **서버 네트워크 어댑터**에는 Server의 실제 어댑터 이름과 IP/프리픽스가 표시됩니다. 자동 모드는 `network.interfaces`를 비우며, 개별 선택은 직접 입력한 이름도 유지합니다. 사용할 수 없는 어댑터와 주소도 표시하지만 UPnP 또는 Google Cast 검색용으로 새로 선택할 수는 없습니다. Cast mDNS는 선택한 인터페이스의 UDP 5353을 사용합니다.
- **재생 기기가 음원을 가져올 Server URL**은 `media.base_url`이며 재생 기기가 Server에서 미디어를 가져올 주소입니다. 같은 주소에서 Web UI를 제공할 수도 있지만 이 설정은 수신 주소, 포트 또는 브라우저 URL을 변경하지 않습니다. 일반적으로 비워 두어 Server가 자동으로 선택하게 하세요. **서버의 네트워크 주소에서 선택**은 감지된 Server IP와 현재 활성화된 HTTP(S) 수신 포트를 조합해 초안만 채우며, 적용하려면 Settings를 저장해야 합니다. 목록은 수신기 접근 가능 여부를 검사한 결과가 아니므로 부적합한 VPN·컨테이너 주소를 고르면 안 됩니다. 직접 입력할 때는 수신기 LAN에서 접근할 수 있는 실제 Server 주소와 활성 수신 포트를 사용하세요. 원격 수신기에 `localhost`나 클라이언트 PC 주소를 사용하면 안 됩니다. 명시적인 값이나 리스너 주소가 있으면 그것이 우선하며, Cast는 Server에서 수신기가 mDNS로 광고한 Cast TCP 포트로 접근할 수도 있어야 합니다.
- 경로 탐색이나 어댑터·IP 선택만으로 저장·재시작·스캔·재생하지 않습니다. 편집 내용을 명시적으로 저장하세요. 리스너·저장 위치·네트워크 변경은 재시작이 필요할 수 있습니다.

### AirPlay 설정 도움말

**AirPlay 송신 프로그램 경로** 옆 **AirPlay 설정 도움말**은 필요한 송신 프로그램을 설명할 뿐 소프트웨어를 설치하거나 AirPlay를 켜거나 임의의 경로를 검증하거나 수신기를 제어하지 않습니다. 지원되는 Linux `amd64`·`arm64` Server 컨테이너에는 `/usr/local/bin/jastreamer-airplay`, 고정된 pyatv 0.18.0을 사용하는 Python 3.12와 FFmpeg가 하나의 일치하는 실행 환경으로 들어 있습니다. 네이티브 Windows Server는 Linux 경로나 다른 임의 송신 프로그램 경로를 입력해도 AirPlay를 지원하지 않습니다.

별도로 설치한 송신 프로그램에는 `main` 브랜치의 임의 버전이 아니라 설치한 Server와 같은 릴리즈의 어댑터 소스와 의존성 파일을 사용해야 합니다. jastreamer와 일치하는 어댑터·통신 규약과 호환 의존성이 필요하며 `atvremote`, Python 실행 파일, pyatv 단독 설치와 Shairport Sync 같은 수신기 소프트웨어로 대신할 수 없습니다. 팝업은 공식 [jastreamer AirPlay 어댑터 소스](https://github.com/furyheimdall/jastreamer/blob/main/apps/server/internal/airplay/helper.py), [고정된 AirPlay 의존성](https://github.com/furyheimdall/jastreamer/blob/main/packaging/server/requirements-airplay.txt), [pyatv 0.18.0 소스](https://github.com/postlund/pyatv/tree/v0.18.0)의 전체 참고 URL을 표시하며, `main` 링크가 설치한 릴리즈와의 호환성을 보장하지는 않습니다. 일반 브라우저에서는 링크를 따라갈 수 있지만 Desktop은 외부 창을 차단할 수 있으므로 열리지 않으면 표시된 전체 URL을 복사해 일반 브라우저에서 여세요. 지원되는 Linux 송신 프로그램 경로를 바꿨다면 저장한 뒤 Server를 재시작하세요.

### 저장한 설정 적용과 서버 재시작

설정 저장 후 재시작이 필요하면 **Settings**에 안내와 **서버 재시작** 버튼이 표시됩니다. 화면을 다시 열어도 안내는 유지됩니다. 저장하지 않은 편집 내용은 먼저 저장하거나 되돌린 뒤 버튼을 누르세요. 확인 창에서 승인하면 재생을 정지하고 저장된 설정으로 Server를 다시 시작합니다. 기존 계정·대기열·플레이리스트는 유지하며 재생은 자동으로 재개하지 않습니다.

같은 주소에서는 연결 복구와 새 Server 실행을 확인한 뒤 완료를 표시합니다. 주소가 바뀌면 안내된 새 주소를 여세요. 실패나 시간 초과는 완료로 표시하지 않으며 재시작 명령을 자동으로 다시 보내지 않습니다. 데이터 디렉터리 변경은 이 버튼으로 적용할 수 없습니다. 기존 데이터를 별도로 옮긴 뒤 Server를 수동으로 재시작하세요. 재시작 API가 없는 이전 Server에는 수동 재시작 안내가 표시됩니다.

### 앨범 아트와 대기열 동작

- 하단 플레이 바의 앨범 아트를 누르면 **대기열**로 이동합니다. 곡 정보를 열거나 재생을 시작하지 않습니다.
- 보관함 `(i)`와 대기열의 앨범 아트는 재생 없이 곡 정보를 엽니다. 대기열 앨범 아트에 마우스를 올리거나 키보드로 포커스하면 큰 `(i)`가 표시되며, 터치 화면에서는 항상 표시됩니다.
- 대기열 행 오른쪽 동작 버튼 중 맨 앞의 삼각형 재생 버튼만 해당 항목을 재생합니다.

재생 기기 상태 조회가 한 번 실패해도 재생을 다시 시작하거나 팝업을 띄우지 않습니다. 조회가 세 번 연속 실패하면 상태 경고를 표시하고, 다음 조회가 성공하면 자동으로 닫습니다. 경고를 직접 닫으면 같은 실패가 이어지는 동안 다시 띄우지 않습니다. 재생 명령 실패와 확인된 연결 끊김은 즉시 표시합니다.

이전 연결에서 저장된 재생 오류는 새 접속마다 ‘요청 실패’ 팝업으로 다시 띄우지 않습니다. 플레이어의 오류 상세에서 확인할 수 있으며, 새로 실행한 요청의 실패는 계속 팝업으로 알립니다.

재생 시작 오류에는 실패 단계(`LoadTrack`, `PrepareMedia`, `SetURI`, `Play`)와 제공 가능한 안전한 오류 코드를 표시합니다. UPnP 명령 거부는 액션 이름과 숫자 오류 코드를, Cast는 액션·player state·idle reason·오류 문구를 보존해 알려주세요. 오류를 지우려고 대기열을 초기화하거나 방화벽을 끄지 마세요. 시간 초과·전송 실패는 여전히 명령 결과 미확정 상태로 처리합니다.

**Server 모드 로컬 오디오**(브라우저 또는 네이티브 Android)에서 재생 중 복구할 수 없는 곡·미디어 엔진 오류가 발생하면 해당 항목에 **재생 실패**를 표시하고 Server가 대기열 순서에 따라 다음 곡을 재생합니다. 정상 재생 완료로 처리하지 않으며 실패 항목·중복 곡·순서를 보존하고 실패한 항목도 직접 다시 재생할 수 있습니다. 다음 곡으로 이어질 때는 조작을 막는 오류 팝업을 띄우지 않으며, 다음 곡이 없으면 오류와 함께 정지합니다. 일시 정지·정지 상태에서 자동 재개하지 않고 네트워크·인증·권한·등록 만료·분류되지 않은 수신기 오류로 곡을 건너뛰지 않습니다. 복구 가능한 손상 프레임 처리는 디코더에 맡기며 임의의 시간만큼 앞으로 탐색하지 않습니다. Android의 이 동작에는 대응하는 Server와 APK가 필요하므로 [업데이트 절차](INSTALL.ko.md#upgrade)에 따라 Server부터 업데이트하세요. 저장된 음악은 대신 로컬 파일용 제한된 실패 처리를 사용하며 로컬 파일 오류를 NAS/Server 원본 오류로 보고하지 않습니다.

UPnP·Google Cast·AirPlay 기능은 수신기마다 다릅니다. 실제 장비에서 소리와 필요한 제어 기능을 확인하세요.


## 2. 문제 해결

| 문제 | 확인할 항목 |
|---|---|
| 웹 화면이 열리지 않음 | Windows Server 콘솔과 TCP 18080, 또는 Linux `/healthz`, Compose 로그, 수신 주소 설정과 TCP 8080/8443 방화벽 확인 |
| 휴대전화 화면이 나타나지 않음 | 기기가 iPhone이거나 브라우저 사용자 에이전트에 Android와 Mobile이 모두 표시되는 휴대전화인지 확인; iPad·Android 태블릿과 폭이 좁은 데스크톱 창은 의도적으로 기존 표준 화면 유지 |
| PWA 설치 동작이 나타나지 않음 | **Settings**를 열고 [PWA 설치](INSTALL.ko.md#pwa)를 따름; 사설 LAN HTTP origin에는 신뢰할 수 있는 HTTPS 요구 사항이 표시됨 |
| Windows에서 서버를 찾지 못함 | mDNS UDP 5353을 허용하거나 완전한 서버 URL 직접 입력 |
| Android에서 WebView 미지원 표시 | 격리 Server 프로필·Server 출력·새 가져오기를 위해 기기의 Android System WebView/Chrome을 업데이트하고 다시 실행; 공용 쿠키로 대신 연결하지 않으며 이미 저장된 음악은 별도 로컬 경로로 유지 |
| Android 서버 검색·재접속 실패 | Server 모드에서 Wi-Fi, mDNS UDP 5353, VPN·기기 격리와 앱 네트워크 접근을 확인하고 완전한 URL 직접 입력; 식별자·TLS 오류를 우회하지 않으며 이 실패가 이미 저장된 음악을 막지는 않음 |
| 출력이 보이지 않음 | UPnP는 SSDP UDP 1900 허용; Google Cast는 선택한 인터페이스의 mDNS UDP 5353과 Server에서 수신기가 광고한 Cast TCP 포트로의 접근 허용; Linux AirPlay는 mDNS UDP 5353과 호스트 네트워크도 필요; Wi-Fi 기기 격리 해제 |
| 출력이 재생하지 못함 | Server→수신기 제어·스트림과 수신기→Server 미디어 HTTP(S) 통신 허용; **재생 기기가 음원을 가져올 Server URL**은 일반적으로 비워 두고 필요할 때만 수신기에서 접근할 수 있는 특정 Server 주소 지정; Cast 원본이 지원되지 않으면 FFmpeg를 설정한 경우에만 변환 사용 |
| Server 모드 **이 기기**에서 소리가 없거나 연결이 끊김 | 표시되면 **재생 허용** 선택, 브라우저·OS 음소거와 출력 경로 확인, 재생 담당 페이지 유지; 새로고침이나 등록 만료 뒤에는 필요하면 정지하고 **이 기기**를 다시 선택해 직접 재생; 디코더·통신 오류 보존 |
| Cast FLAC이 EOF 부근 탐색 뒤 실패함 | 보고된 `BUFFERING`/`ERROR`를 보존; 독립 재현된 수신기 의존 실패이며 완료가 아니므로 대기열 항목을 건너뛰거나 소유 세션의 `FINISHED` 조건을 약화하지 않음 |
| 보관함이 비어 있음 | Windows 폴더 또는 Linux `/music` 마운트가 설정된 보관함인지, Server 계정/UID 10001의 읽기·탐색 권한과 스캔 완료 여부 확인; 포함된 `jastreamer-samples`도 명시적으로 스캔해야 함 |
| 설정 저장 실패 | Windows 나란한 파일 또는 Linux config 폴더와 `server.json`을 Server 계정/UID 10001이 쓸 수 있는지 확인 |
| AirPlay 인증 실패 | 지원되는 Linux Server 전용: 재생 정지, 표시된 PIN·암호 재입력, 패키지의 `/usr/local/bin/jastreamer-airplay`·FFmpeg 경로 유지; 네이티브 Windows는 다른 경로로 AirPlay를 활성화할 수 없음 |
| 비밀번호 분실 | 서버를 멈추고 같은 config/data를 연결한 유지보수 컨테이너에서 `jastreamer-server --reset-password USER --config /etc/jastreamer/server.json` 실행; 새 비밀번호는 입력 프롬프트에만 입력 |
| Android 화면을 끈 뒤 재생이 멈추거나 로컬 출력이 오프라인이 됨 | 가능하면 재연결 전에 발생 시각과 `<data_dir>/logs/server.log*` 보존. Server는 등록·갱신 만료, 마지막 명령 조회·갱신·상태 보고 시각, 명령 결과와 미디어 접근 권한·전송 실패를 기록함. 서버가 관찰한 단계는 확인할 수 있지만 Android가 앱을 절전·종료한 원인까지 확정하는 로그는 아님 |
| Android에 재생 오류가 표시되지만 출력은 계속 사용 가능함 | `<data_dir>/logs/server.log*`의 `native_playback_error` 확인. 대응하는 Android/Server 버전은 보고된 재생 예외를 출력·재생·명령 ID와 함께 자동 보관하며, 재생 실패 자체가 등록 상실을 뜻하지는 않음 |

문제를 보고할 때 Server 버전, 정확한 이미지 다이제스트 또는 Windows ZIP SHA-256, Server 플랫폼·아키텍처, 관련 로그와 수신기 모델을 포함하세요. 비밀번호, 쿠키, 인증서와 개인 키는 제거하고 원본 진단 문구는 그대로 보존하세요.

Server 진단 로그는 UTC 시각으로 콘솔과 `<data_dir>/logs/server.log`에 함께 기록됩니다. 기본 Linux 컨테이너 경로는 `/var/lib/jastreamer/logs/server.log`이며, 기존 데이터 마운트에 저장되어 컨테이너를 교체해도 남습니다. 현재 파일과 `server.log.1`–`server.log.3`을 각각 최대 5 MiB로 순환 보관합니다. 정상 로컬 출력 활동은 등록별 최대 30초에 한 번 요약하고 반복 요청 실패는 기록 빈도를 제한합니다. 진단 이벤트는 생성된 출력·재생·명령 ID로 연관 관계를 남기며, 인증정보·쿠키·미디어 URL·파일명·출력 표시 이름은 기록하지 않습니다. POSIX 시스템에서는 로그 디렉터리를 0700, 파일을 0600 권한으로 생성하며, Windows에서는 데이터 디렉터리의 접근 권한을 Server 계정으로 제한하세요. 영구 로그 기록을 시작하지 못하면 Server 콘솔이나 Compose 로그를 확인하세요. 정확한 발생 시각과 남아 있는 로그 파일 전체를 함께 수집하고 데이터 디렉터리를 HTTP로 공개하지 마세요.

Server 모드 Android 재생 오류 진단에는 대응하는 Server와 APK가 필요합니다. [업데이트 절차](INSTALL.ko.md#upgrade)에 따라 호환 Server를 먼저 업데이트한 뒤 해당 APK를 설치하세요. 실패한 네이티브 명령과 최종 재생 오류 보고에는 Media3 오류 코드·이름, 발생 시각, 재생 위치, 원인 예외 타입과 길이가 제한된 코드 심볼 기반 호출 스택이 포함됩니다. 확인 가능한 HTTP 상태와 코덱·오디오 출력의 숫자 오류 코드도 함께 남깁니다. 예외 메시지 원문, 미디어 URL, 파일 경로와 인증정보는 제외합니다. Server가 받은 보고는 별도 ADB 수집 없이 기존 순환 로그에 자동 보관합니다. 오프라인 업로드 대기열은 아니므로 전달 전에 프로세스가 종료되거나 Server 연결이 끊기면 수집되지 않을 수 있으며, 이전에 유실된 예외를 복원하지는 못합니다. 저장된 음악 오류는 기기 내 로컬 진단에만 남고 자동 업로드하지 않습니다.
