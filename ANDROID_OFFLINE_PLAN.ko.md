# Android 독립 오프라인 플레이어 구현·검증 계획

상태: **현재 Android·Server·Web 소스에 구현됨, 릴리즈 검증 대기 중**. 소스 동작을 설명하지만 게시된 production 릴리즈나 병합·서명·설치·배포·공개 승인을 뜻하지 않습니다. CI APK는 개발·테스트 산출물이며 실물 휴대전화 설치·네트워크, 원본/AAC 가청 재생, Bluetooth·헤드셋 동작, 동일 서명 업데이트 보존은 아직 실물 검증을 마치지 않았습니다. 현재 사용자 동작은 [사용자 안내서](INSTRUCTION.ko.md#android-controls)에 요약되어 있습니다. [English](ANDROID_OFFLINE_PLAN.md).

## 1. 최신 결정과 대체된 방향

2026-09-21 사용자의 최종 정정이 이전 선택보다 우선한다. 핵심은 **서버의 오프라인 cache가 아니라, 서버에서 음악을 가져와 독립적으로 보관하는 플레이어**이다.

| 결정 | 현재 확정 방향 |
|---|---|
| D1 | 기본 진입점은 앱 자체 서버 선택 화면이다. 여기에서 **저장된 음악 듣기**로 바로 들어간다. 서버 검색·연결과 오프라인 플레이어는 서로 다른 경로다. |
| D2 | 저장된 곡·앨범·플레이리스트 화면과 재생 엔진은 앱에 포함된다. 화면 표시·검색·재생에 서버 HTML, 로그인, 서버 응답이 필요하지 않다. |
| D3 | 서버의 곡·앨범·플레이리스트에서 다운로드할 수 있고 원본과 용량 절약 음질을 제공한다. |
| D4 | 다운로드를 검증해 로컬 보관함에 가져오기 완료한 뒤에는 서버·계정의 수명과 관계없는 기기 소유 음악이다. 서버 로그아웃·계정 삭제·프로필 제거·접속 실패가 음악을 잠그거나 삭제하지 않는다. |
| D5 | 서버 플레이리스트 다운로드는 그 시점의 목록을 가져오는 일회성 작업이다. 완료된 음악·목록은 자동 동기화, 자동 삭제, 자동 갱신하지 않는다. 사용자가 오프라인 플레이어에서 직접 관리한다. |
| D6 | 오프라인 대기열·플레이리스트·현재 위치는 기기가 관리한다. 원래 어느 서버에서 받았든 저장곡을 함께 들을 수 있으며 서버 공용 대기열·출력·재생에는 반영하지 않는다. |
| D7 | Android를 먼저 구현했다. Windows와 iOS 오프라인 플레이어 구현·패키지 변경은 이번 범위 밖이다. NAS 원본은 변경하지 않으며 기존 Server 제어 기능은 유지한다. |

**폐기한 이전 방향:** 기본 온라인/오프라인 통합 스트리밍 화면, 서버별 오프라인 접근 프로필, 로그아웃 시 완료 음악 잠금, 다운로드 플레이리스트 자동 동기화, 재연결 시 로컬 오류의 서버 자동 업로드. 원본과 Server가 광고하는 AAC-LC 256 kbps 선택, 기본 비과금망 정책과 아래 네이티브 화면은 구현된 소스 동작이지만 게시·실물 검증을 마친 릴리즈라는 뜻은 아니다.

## 2. 진입 화면과 서버 디스커버리

```text
앱 실행 → 앱 자체 서버 선택 화면
          ├─ 저장된 음악 듣기 [항상 접근 가능]
          │  └─ 독립 오프라인 플레이어
          │     ├─ 보관함: 저장된 곡 / 앨범 / 아티스트 / 폴더 / 검색
          │     ├─ 플레이리스트: 기기에 저장된 목록
          │     ├─ 대기열: 기기 대기열
          │     └─ Settings: 기기 저장 공간·언어·진단
          ├─ 다운로드 관리: 사용자가 요청한 전송 작업
          ├─ 저장된 서버 → 확인·필요 시 로그인 → 기존 서버 화면
          ├─ 주변에서 발견한 서버 → 확인·로그인 → 기존 서버 화면
          └─ 주소로 추가 → 확인·로그인 → 기존 서버 화면
```

### 앱 자체 서버 선택 화면

- 콜드 스타트 기본 화면이며 Android 디스커버리·최근 서버·주소 입력을 재사용한다. 살아 있는 Activity로 돌아오거나 회전하면 현재 화면을 보존한다.
- 위쪽에 큰 **저장된 음악 듣기** 버튼과 로컬 곡·앨범 수가 표시된다. 로그인 계정명이나 마지막 Server 접속 성공 여부는 활성 조건이 아니다.
- 저장된 곡이 없어도 버튼이 열리며 로컬 빈 화면에서 **서버에 연결해 음악 다운로드**로 돌아갈 수 있다. 로컬 화면을 표시하기 위해 먼저 로그인할 필요가 없다.
- 그 아래 저장된 서버, 현재 LAN에서 발견한 서버, 검색 갱신·실패 안내와 완전한 HTTP(S) 주소 입력을 제공한다. 서버 이름·주소·확인 중/연결 가능/연결 불가 상태를 표시한다.
- 온라인 접속은 Server UUID·origin 검증과 해당 서버 인증 후 진행한다. 이름만 같다고 기존 자격 증명을 재사용하지 않는다. 비행 모드나 검색 실패는 서버 경로에만 영향을 주며 저장된 음악 경로를 막지 않는다.
- 인터넷 전체의 서버 검색, NAS 외부 공개, VPN·방화벽 자동 변경은 하지 않는다. 오프라인 플레이어로 이동하면 화면용 주변 검색을 중지하며, 사용자가 이미 요청한 다운로드는 별도 작업으로 관리한다.

### 오프라인 플레이어와 돌아오기

- 상단 제목은 **저장된 음악**이며 선택한 Server/계정을 재생 소유자처럼 표시하지 않는다. **서버 목록**으로 돌아가는 동작과 **다운로드** 진입점이 있다.
- 네 탭은 **라이브러리 / 재생목록 / 대기열 / 설정**이다. 로컬에 저장된 음악 전체를 표시하며 Server 필터나 로그인 조건이 없다.
- 다운로드 목록·Server 화면으로 이동해도 현재 로컬 재생을 멈추지 않으며 화면 이동 때문에 다른 네트워크 출력에 Stop을 보내지 않는다.
- 네이티브 레이아웃은 휴대전화·태블릿·접힘, 가로 화면, 글꼴 확대, TalkBack과 키보드 경로를 구현하며 80dp 미니 플레이어, 최소 48dp 터치 영역과 한 번만 적용하는 시스템 여백을 사용한다. 이 기기·접근성 경로는 실물 시험 완료 주장이 아니라 릴리즈 검증 대상이다.
- 계정·로그아웃은 서버 접속 문맥에 표시한다. 독립 플레이어의 음악을 듣기 위해 계정 메뉴를 거치지 않는다.

## 3. 다운로드 완료가 소유권 경계다

구현은 세 가지 상태를 구조적으로 분리한다.

| 데이터 | 소유·수명 |
|---|---|
| 서버 프로필·세션 | 서버 검색·온라인 접속용. origin·Server UUID·계정으로 격리 |
| 미완료 다운로드 작업 | 작업을 시작한 서버·계정과 음질·원본 버전에 연결. 인증·전송·검증 중에만 필요 |
| 완료된 로컬 음악·목록 | 기기 로컬 ID로 관리. 서버 프로필·계정 삭제의 연쇄 삭제 대상이 아님 |

```text
인증된 서버에서 요청
 → 임시 파일 수신
 → 길이·checksum·선택한 산출물 검증
 → 로컬 파일·메타데이터·로컬 ID를 원자적으로 확정
 → 저장된 음악에 공개
 → 전송용 URL·자격 증명 연계와 작업의 활성 서버 연결 제거
```

- 완료된 곡·앨범·플레이리스트는 로컬 ID, 파일 무결성 정보, 표시 메타데이터, 실제 codec/음질과 기기 내 참조로 관리한다. Server 곡/앨범/플레이리스트 ID나 계정 ID를 접근·갱신·삭제 키로 사용하지 않는다.
- 원래 음원의 제목·아티스트·앨범·디스크/트랙 번호·아트는 파일 또는 가져온 데이터의 **로컬 사본**이다. 원격 artwork URL을 매번 요청하지 않는다. 이미지 실패 시 기본 그림을 쓰며 오디오를 막지 않는다.
- 완료 이력은 로컬 파일명·완료 시각·결과 중심으로 남긴다. 재생·동기화·자동 재다운로드를 위해 원격 주소, 만료 미디어 URL, 계정 자격 증명을 완료된 보관함에 남기지 않는다. 원본 파일 자체에 이미 들어 있는 태그를 지우기 위해 음원을 수정하지 않는다.
- 파일과 DB 사이에서 앱이 종료되어도 확정된 로컬 음악을 잃지 않도록 복구한다. 소유권 전환은 단순히 마지막 바이트를 받았다는 사실이 아니라 검증·로컬 등록 완료로 판정한다.
- 다운로드 완료와 로그아웃이 동시에 발생하면 완료 확정 경계를 원자적으로 판정한다. 이미 확정된 파일은 보존하고, 취소된 작업이 폐기된 인증을 다시 사용하지 못하게 한다.

## 4. 음악을 가져오고 듣는 워크플로우

### 서버에서 곡·앨범을 가져오기

1. 서버 선택 화면에서 서버를 확인하고 로그인한다.
2. 기존 서버 보관함의 곡 행·앨범 카드·상세 화면에서 다운로드를 선택한다. 다운로드는 재생 또는 서버 대기열 변경을 일으키지 않는다.
3. 원본/용량 절약, 곡 수, 저장 위치와 현재 네트워크 정책이 데이터 제한 없는 연결을 기다릴지 확인한다. 실제 전송 용량은 작업 진행 중 다운로드 화면에 표시된다.
4. 진행 상태는 서버 화면과 앱 자체 다운로드 관리에서 확인한다. 다른 화면에서 이미 저장된 음악을 듣는 동안에도 허용된 작업은 진행할 수 있다.
5. 곡별 완료 후 **저장된 음악에서 보기**로 이동한다. 서버에서 로그아웃한 뒤에도 그대로 남아 재생된다.

### 서버 플레이리스트 가져오기

1. 사용자가 다운로드를 누른 시점의 확정된 서버 목록·순서·중복을 스냅샷으로 받는다.
2. 필요한 오디오를 받아 **로컬 플레이리스트**를 만든다. 묶음 도중 실패한 항목은 곡명과 사유를 로컬에 남기고 **10곡 중 7곡 저장됨**으로 표시한다.
3. 실패·미완료 항목은 재생 가능한 것으로 취급하지 않는다. 유효한 작업이 남아 있을 때만 다운로드 관리에서 재시도할 수 있다. 작업을 취소·종료한 후 다시 받으려면 사용자가 서버에서 새 요청을 한다.
4. 완료된 로컬 목록은 원래 서버 플레이리스트와 연결되지 않는다. 서버에서 곡을 추가·제거·재정렬·삭제해도 바뀌지 않는다.
5. 나중에 Server 플레이리스트를 다시 내려받으면 이름이 같아도 별도 로컬 재생목록을 만드는 **새 가져오기**이다. 기존 로컬 목록을 자동 교체하거나 추가하지 않는다. 이미 저장된 동일 파일은 checksum·음질을 검증한 뒤에만 재사용할 수 있다.

### 비행 모드에서 듣기

1. 앱 실행 → **저장된 음악 듣기**를 누른다. 서버 로그인·프로필 선택·네트워크 확인을 기다리지 않는다.
2. 곡·앨범·아티스트를 탐색하거나 로컬 메타데이터로 검색한다. 다른 서버에서 가져온 음악도 같은 로컬 보관함에 표시한다.
3. 곡 또는 앨범 재생을 누르면 기기 대기열에 순서와 중복을 유지하여 재생한다. 일부만 가져온 묶음은 **저장된 7곡 재생**처럼 범위를 명시한다.
4. 미니 플레이어를 열어 전체 화면, 탐색, 이전/다음, 셔플·반복과 대기열을 사용한다. 잠금 화면·헤드셋·Bluetooth 제어도 로컬에서 처리한다.
5. 앱이나 기기를 다시 시작하면 목록·대기열·위치는 복원되지만 사용자 요청 전에는 소리를 내지 않는다.

### 로컬에서 관리하기

- 로컬 플레이리스트를 만들고 이름·순서·곡 구성을 바꿀 수 있다. 오프라인 변경은 서버에 전송하지 않는다.
- **목록에서 제거**, **로컬 플레이리스트 삭제**, **기기에서 음원 삭제**를 구분한다. 목록 편집만으로 공유 파일을 몰래 지우지 않는다.
- 명시적인 음원 삭제는 영향을 받는 로컬 목록을 안내하고 확인한다. 같은 파일을 참조하는 목록을 일관되게 갱신하며 NAS나 서버 플레이리스트는 바꾸지 않는다.
- 현재 재생 중인 파일은 기본적으로 사용이 끝난 뒤 삭제한다. **지금 정지하고 삭제**는 별도 명시적 선택이다.

### 선택·삭제·폴더 정리

- 곡·앨범·폴더를 길게 눌러 선택 모드에 들어간다. 다중 선택과 현재 화면 전체 선택에서 선택 곡 수·중복을 제외한 파일 용량을 표시한다. 폴더 화면은 조회 전용이 아니라 정리·이동 동작을 제공한다.
- **기기에서 삭제**는 로컬 음원의 명시적인 영구 삭제다. 확인창에 곡 수·확보 용량·영향받는 플레이리스트와 대기열을 안내한다. 휴지통·되돌리기·서버 자동 복구를 약속하지 않는다. 재생 중인 곡에는 **재생 종료 후 삭제 / 지금 정지하고 삭제 / 취소**를 제공한다.
- 삭제 예정인 재생곡은 현재 사용 종료까지 보존하고 이후 반복 모드로 다시 시작하지 않는다. 파일을 실제 삭제하면 참조하는 모든 로컬 목록·대기열을 일관되게 갱신한다. 일부 삭제 실패는 항목별로 남기고 전체 성공으로 표시하지 않는다.
- **폴더**는 앱이 소유한 지속 음악 저장소 안의 실제 파일 배치다. 가상 태그만 바꾸고 파일을 이동한 것처럼 표시하지 않는다. 휴대전화 전체 파일 관리자나 NAS 폴더 탐색과는 별개다.
- **새 폴더**, **폴더 이름 변경**, **선택한 곡 이동**, **폴더 이동**, **폴더 삭제**를 제공한다. 하위 폴더·경로 탐색·상위로 이동·목적지 선택을 지원하고 기본 저장 위치는 앱의 **가져온 음악** 폴더로 한다. 사용자가 다운로드 요청 시 다른 로컬 폴더를 선택할 수 있다.
- 이동은 선택 → **이동** → 목적지 폴더 선택 또는 생성 → 완료 흐름이다. 로컬 곡 ID를 유지하면서 실제 파일 경로와 색인을 갱신하고 기존 재생목록·대기열·재생 위치를 보존하며 곡을 새 항목으로 복제하지 않는다. 사용 중 파일도 활성 핸들과 대기 참조를 유지해 몰래 재생을 끊지 않는다.
- 같은 이름의 다른 파일은 덮어쓰지 않는다. **이름 바꾸어 이동 / 건너뛰기 / 나머지 취소**를 제공한다. 동일 파일 이동과 중복 선택은 중복 작업으로 처리하지 않는다. 이름이 같다는 이유만으로 내용을 합치지 않는다.
- 폴더 이동·이름 변경은 경로만 바꾼다. 제목·아티스트·앨범 태그와 음원 바이트는 수정하지 않는다. 앨범 보기와 폴더 보기는 다른 분류이며 폴더 이름 변경으로 앨범 태그가 바뀌지 않는다.
- 이동 중 앱 종료·디스크 오류가 나도 원본과 로컬 색인을 잃지 않도록 복구한다. 복사가 필요한 경로라면 목적지 검증 전에 원본을 지우지 않는다. 진행·성공·실패를 실제 결과대로 표시한다.
- 관리 범위는 앱 음악 루트 내부로 한정한다. 루트 밖 이동, 경로 탈출, 심볼릭 링크를 통한 우회, 자기 자신이나 하위 폴더로의 순환 이동은 거부한다. 내부 임시 다운로드·DB·쿠키·다른 앱 파일은 폴더 보기에 노출하지 않는다.
- 진행 중 다운로드의 목적지 폴더는 안정된 로컬 폴더 ID로 추적한다. 이름/위치 변경을 따라가며, 삭제되면 작업을 대기시키고 새 저장 위치를 받는다. 지운 폴더를 몰래 다시 만들거나 완료 파일을 잃지 않는다.

## 5. 다운로드 버튼과 상태

| 상태 | 표시·동작 |
|---|---|
| 요청 가능 | 다운로드 버튼. 서버 연결·인증·형식 기능에 따른 사용 가능 여부와 이유 표시 |
| 대기 | 네트워크 정책·연결·변환·작업 대기 사유, 취소 |
| 진행 | 진행 링, 퍼센트 또는 받은 용량, 일시 중지·취소 |
| 일시 중지·실패 | 사용자 중지·공간·인증·파일 변경·전송 실패를 구분하고 허용된 재시도 제공 |
| 가져오기 완료 | 로컬 저장 완료 및 **저장된 음악에서 보기**. 계정 잠금 상태 없음 |
| 일부 완료 | 묶음의 성공/실패 곡 수와 이미 저장된 곡 듣기 |

- 곡·앨범·플레이리스트 목록과 상세의 다운로드 버튼은 재생·화면 열기와 별도의 터치 대상이며 매 바이트를 읽어주지 않는 한국어·영어 이름을 사용한다.
- Server 화면은 현재 살아 있는 문서가 요청한 작업만 표시한다. Server 곡 ID나 같은 이름으로 영구적인 전체 **이미 저장됨** 배지를 추측하지 않는다.
- 명시적 가져오기 중 manifest checksum과 실제 음질이 일치하면 검증된 기존 로컬 파일을 재사용해 중복 저장을 피할 수 있다. 같은 이름이나 Server 곡 ID만으로 다른 파일을 합치지 않으며, 하나의 검증 파일을 참조하더라도 재생목록·대기열의 반복 항목은 별도로 유지한다.
- 서버가 내주는 웹 화면의 다운로드 기능은 호환되는 Android의 제한된 가져오기 기능에만 연결한다. 일반 브라우저나 다른 셸에 임의 파일 저장·네이티브 권한을 열어주는 변경이 아니다.

## 6. 음질·전송·기기 저장소

### 음질

- **원본:** 원본 바이트 그대로 저장한다. Android 디코더 호환이나 bit-perfect 출력을 보장하는 표현은 아니다. 지원하지 않는 형식은 안내하고 다른 음질로 몰래 대체하지 않는다.
- **용량 절약:** Server가 생성하는 산출물은 탐색 가능한 AAC-LC 256 kbps M4A이다. mono/stereo를 유지하고 더 많은 채널은 명시적으로 stereo 변환하며 불필요한 업샘플링 없이 44.1/48kHz를 지원한다. 실제 codec·음질·용량을 기록한다.
- 작은 저비트레이트 원본보다 변환 결과가 클 수 있으므로 예상값과 실제값을 구분한다. Server는 설정한 미디어 실행 환경이 지원할 때만 AAC를 광고하므로 호환되지 않는 Server에서는 원본만 제공한다.
- 기본 음질을 바꿔도 이미 저장된 파일은 바뀌지 않는다. 다른 품질이 필요하면 서버에서 새로 가져온 뒤 사용자가 기존 로컬 사본의 보존·삭제를 결정한다.

### 진행 중인 전송

- 기본은 데이터 제한이 없는 네트워크다. 사용자가 **데이터 제한 또는 모바일 데이터 허용**을 직접 켜야 하며 현재 네트워크가 정책에 맞지 않으면 전송을 대기시킨다.
- 동시 전송·서버 변환 작업 수를 제한하고 기존 재생 자원을 우선한다. 다운로드 때문에 전체 보관함 분석을 다시 시작하지 않는다.
- 진행 중인 작업만 원본 버전·Range 조건·인증을 확인해 이어받는다. 바뀐 원본에 옛 부분 파일을 이어 붙이지 않는다. 인증·TLS·Server 식별자 오류는 무한 재시도하지 않는다.
- 임시 파일의 길이·checksum을 검사해 로컬 등록한다. 전송 checksum은 모든 디코더에서 끝까지 재생된다는 증거가 아니다. 알려진 동일 소스 버전의 검증 실패는 새 가져오기를 막고 이유를 표시하며, 기존 로컬 사본에는 소급 적용하지 않는다.
- Android 버전에 맞는 영속 작업과 알림을 사용하고 재생용 foreground service를 전송 권한 대용으로 쓰지 않는다. 강제 중지 뒤 자동 재시작은 보장하지 않으며 다음 앱 실행에서 미완료 작업을 복구한다.

### 완료된 저장소

- 완료곡은 앱 전용 **지속 저장소**에 두며 OS의 임시 cache 정리 대상에 넣지 않는다. 서버 Web 프로필·쿠키·최근 서버 저장소와 분리한다.
- 파일·로컬 곡·앨범·플레이리스트는 로컬 ID와 checksum으로 관리한다. 계정/Server 프로필에 대한 cascade-delete 관계를 두지 않는다.
- 명시적 다운로드에 LRU 자동 삭제, 서버 변경에 따른 자동 교체, 만료 URL 갱신이나 온라인 사용권 검사를 적용하지 않는다. 사용자 삭제 외에는 앱이 완료곡을 임의로 제거하지 않는다.
- Settings에 사용량·여유 공간·선택 가능한 다운로드 한도·개별/전체 음악 삭제를 제공한다. 공간 부족 시 새 전송을 대기시키고 사용자가 정리할 수 있게 한다. 한도 미설정 상태도 안전 여유 공간을 검사한다.
- 부분 파일과 앱 소유 임시 산출물 정리는 완료된 음악과 구분한다. 로컬 재생 목록에서 빠졌다는 이유만으로 완료 파일을 자동 회수하지 않는다.
- 앱 삭제·Android 앱 데이터 초기화·기기 저장소 손실까지 견디는 백업을 의미하지 않는다. 같은 application ID·같은 서명의 덮어쓰기 업데이트는 저장 음원·앨범 아트·로컬 재생목록·대기열/위치·폴더·환경설정을 보존한다. 앱 제거 또는 **저장 공간/데이터 지우기**는 앱 소유 비공개 보관함과 상태를 삭제하므로 서명 불일치 업데이트를 강행하는 방법으로 사용하지 않는다. 파일 내보내기·백업·임의 로컬 파일 가져오기는 별도 범위다.

## 7. 로그인과 서버 변화의 영향

| 사건 | 진행 중인 다운로드 | 완료된 오프라인 음악 |
|---|---|---|
| 연결 끊김·NAS 중단 | 정책에 따라 대기·이어받기 | 영향 없음 |
| 세션 만료 | 새 원격 요청은 같은 계정 재인증 필요 | 영향 없음 |
| 명시적 서버 로그아웃 | 해당 세션을 사용하는 전송 중지, 인증 사용 종료 | 잠금·삭제·재생 정지 없음 |
| 서버 계정 삭제·권한 철회 | 이후 인증 요청은 거부됨 | 소급 잠금·삭제 없음 |
| 최근 서버 바로가기 제거 | 영향 없음. 격리 프로필·세션과 해당 작업은 유지 | 영향 없음 |
| 격리 프로필을 사용할 수 없거나 제거함 | 미완료 원격 작업은 계속하거나 자격 증명을 재사용할 수 없음. 이미 시작한 로컬 확정만 복구 | 영향 없음 |
| 서버 곡·앨범·플레이리스트 수정/삭제 | 작업이 참조한 버전 변경·실패를 처리 | 자동 동기화·삭제 없음 |
| 다른 서버나 다른 계정 접속 | 새 작업은 그 인증 범위로 격리 | 기존 로컬 음악과 함께 사용 가능 |

- 다운로드한 파일의 접근권은 서버 계정 접근권과 별개다. 같은 기기·앱을 사용하는 사람은 이전 계정으로 내려받은 음악도 들을 수 있다. 로그아웃은 로컬 음악을 숨기거나 보호하는 수단이 아니며 기기 잠금 등으로 접근을 관리한다. 서버에서 원격 회수할 수 있다는 약속을 하지 않는다.
- 서버 로그아웃 때문에 정지해야 하는 기존 **Server 소유 Android 렌더러**와 계정이 없는 **오프라인 플레이어**를 구별한다. 로컬 소유 상태에서 서버 로그아웃이 발생해도 로컬 음악은 계속 재생한다. 다른 네트워크 출력에 임의 Stop을 보내지 않는다.
- 비행 모드의 서버 로그아웃은 로컬 인증 사용을 끝낼 수 있지만 도달하지 못한 서버 세션 폐기까지 성공했다고 표시하지 않는다. 이는 완료된 음악과 무관하다.
- 내려받은 음악을 삭제하려면 오프라인 플레이어의 명시적인 삭제 기능을 사용한다. **로그아웃**, **서버 삭제**, **cache 지우기**를 로컬 음악 전체 삭제의 우회 동작으로 만들지 않는다.

## 8. 단일 Media3 엔진과 대기열

- 기존 Media3 서비스·MediaSession을 활용하되 **Server 렌더러 소유**와 **오프라인 플레이어 소유**를 명시적으로 분리한다. 엔진 두 개를 동시에 실행하거나 늦게 도착한 Server 명령을 로컬 재생에 적용하지 않는다.
- Server 화면에서 기존 방식으로 재생할 때는 Server 공용 대기열·명령·lease 정책을 그대로 유지한다. 이번 방향은 서버 온라인 재생을 독립 기기 스트리밍으로 바꾸는 요구가 아니다.
- 오프라인 플레이어는 로컬 파일만 재생한다. 없거나 손상된 파일 때문에 원래 NAS에 자동 접속하거나 자동으로 다시 내려받지 않는다. 위치·다음 곡·셔플·반복·시스템 제어 모두 기기에서 실행한다.
- 서로 다른 서버에서 받았던 곡도 같은 로컬 대기열에 담을 수 있다. 곡 ID는 기기 로컬 ID이며 순서·중복·현재 항목·위치를 영속화한다. 위/아래 이동·제거, 반복 끔/한 곡/전체, 셔플을 제공한다. 볼륨은 Android 시스템 제어를 사용한다.
- 다른 서버를 찾아보거나 네트워크 출력을 제어하는 것은 로컬 재생 중단 사유가 아니다. 해당 Android를 Server 출력으로 지정하거나 Server 소유 Android 재생에서 로컬 곡을 시작할 때만 명시적인 소유권 전환을 확인한다.
- 전환 시 기존 소유자의 실제 로컬 엔진 정지를 확인하고 상태를 보존한 뒤 다음 소유자에게 넘긴다. Server Stop 응답이 없으면 성공을 꾸며내지 않으며 옛 등록·명령이 새 엔진을 조작하지 못하게 한다. Server 재생을 자동으로 로컬 대기열에 복제하지 않는다.
- 오디오 포커스·전화·헤드폰 분리·Bluetooth 변경은 Android 정책을 따른다. Activity 재생성은 트랙 재시작 계기가 아니다. 프로세스 재시작은 상태만 복원하고 자동 재생하지 않는다.

## 9. 구현된 Server·앱 계약

현재 소스는 [HTTP API 계약](contracts/http-api)에 플랫폼 공용 versioned v1 계약을 정의하고 Android 가져오기에 사용한다. 미래의 네이티브 iOS 클라이언트가 사용할 수 있는 공용 계약이지만 이번 작업에는 **iOS 오프라인 구현이 없다**. 게시된 구형 Server에는 이 기능이 없을 수 있다.

- **인증된 다운로드:** 공용 재생 대기열이나 출력 등록을 바꾸지 않고 원본 또는 변환 음원을 가져온다. 기존 재생 lease에 묶인 `/media/` URL은 영구 음악 ID가 아니다.
- **manifest/Range:** 작업 중 일관된 소스 버전, 선택 음질, MIME, 길이, checksum, Range 검증자와 복사할 메타데이터·아트를 전달한다. 작업에 필요한 원격 식별자는 완료 보관함의 수명 연결고리가 되지 않는다.
- **목록 스냅샷:** 명시적 가져오기 시점의 플레이리스트 이름·순서·revision·중복을 원자적으로 보존하며 이후 변경을 구독하지 않는다.
- **선택적 변환 작업:** 제한된 작업, 취소·상태·실패·불변 산출물·유한 Server 보관과 권한 검사를 제공한다. 전송 산출물 만료는 이미 확정한 로컬 파일에 영향을 주지 않는다.
- **제한된 Android 가져오기 인터페이스:** 검증된 현재 Server 최상위 문서만 명시적 다운로드와 필요한 작업 상태를 요청할 수 있다. 임의 URL·경로·파일 목록·자격 증명 또는 완료 보관함 전체 조회·삭제 권한은 제공하지 않는다.
- **기능 협상:** 새 가져오기는 호환 v1 Server/Web 지원이 필요하다. 구형 Server는 자신이 지원하는 기존 제어 기능을 유지하며 저장된 음악을 막거나 완료 가져오기를 변경할 수 없다.
- **로컬 저장 계약:** 인증이 필요한 진행 작업과 완료된 로컬 파일·DB 상태를 분리한다. 앱 자체 Kotlin 음악 UI는 Server HTML이나 service worker cache 없이 시작한다.

현재 원격 Web의 임의 다운로드·외부 탐색 차단과 sandbox를 일반적으로 해제하지 않는다. 다른 origin redirect, 잘못된 TLS, Server 식별자 변경은 허용하지 않는다. 일반 HTTP는 여전히 신뢰하는 LAN 안에서만 사용한다.

## 10. 오류와 진단

- 전송 오류·인증 필요·공간 부족·원본 변경·checksum 불일치·지원하지 않는 형식·로컬 디코딩 실패를 구분한다. 부분 파일을 저장 완료로 표시하지 않는다.
- 로컬 파일 누락·손상은 오프라인 플레이어에서 표시하고 사용자가 해당 항목을 정리하거나 서버에서 다시 가져올 수 있게 안내한다. 앱이 원래 서버를 기억해 자동 복구하는 구조는 아니다.
- 확정된 로컬 미디어 디코딩 실패만 해당 기기 대기열 항목에 기록하고 다음 재생 가능한 항목으로 한 번 진행할 수 있다. 권한·원인 미확정 오류는 무작정 건너뛰지 않는다. 반복 모드도 실패곡을 무한 순환하지 않으며 한 순회에서 모두 실패하면 정지한다.
- 오프라인 플레이어 오류·파일 상태는 **기기에 기록**한다. 재연결 시 서버에 자동 업로드하거나 Server 렌더러 오류·NAS 원본 검증 실패로 변환하지 않는다. 필요한 진단 공유는 사용자의 별도 명시적 내보내기로 다룬다.
- 실제 로컬 곡 ID·codec·음질·엔진 오류를 기록하되 쿠키·토큰·비밀번호를 기록하지 않는다. 서버의 기존 렌더러 오류 이력은 기존 Server 소유 재생에 계속 적용한다.

## 11. 수용 조건

아래는 구현된 소스의 릴리즈 검증 시나리오이며 production 릴리즈나 모든 실물 기기 경로가 통과했다는 뜻이 아니다. 특히 emulator·소스 검증만으로 실물 설치·가청 출력·재부팅/업데이트 보존·Bluetooth/헤드셋·접힘/접근성·실제 소유권 경합 처리를 입증할 수 없다.

| ID | 시나리오와 관찰할 결과 |
|---|---|
| AC01 | 첫 실행·서버 중단·비행 모드에서도 앱 자체 서버 선택과 저장된 음악 화면이 열림. 로그인·서버 HTML이 선행 조건이 아님. 빈 보관함도 명확히 표시. |
| AC02 | LAN 발견·최근 서버·직접 주소·Server 식별자 검증·로그인·취소 흐름 유지. 이 경로의 실패가 로컬 보관함 접근을 막지 않음. |
| AC03 | 곡·앨범·플레이리스트에서 다운로드 요청·진행·완료·부분 실패 표시. 버튼이 재생·화면 열기·공용 대기열 변경을 발생시키지 않음. |
| AC04 | 원본 checksum 일치 및 실제 표시한 AAC 음질의 탐색·끝까지 재생. NAS 원본 불변. |
| AC05 | 완료 후 비행 모드에서 앱 종료·재실행·기기 재부팅 뒤 검색·선택·탐색·다음 곡 재생. 서버 요청 없이 동작하며 자동 재생 없음. |
| AC06 | 명시적 서버 로그아웃·세션 만료·계정 삭제·권한 철회 후에도 완료 음악의 목록·아트·재생 접근이 그대로 유지됨. 재로그인 잠금 없음. |
| AC07 | 최근 서버/프로필 제거와 Web cookie/cache 정리가 완료 음악을 삭제하지 않음. 다른 계정 접속 후에도 로컬 음악 사용 가능. |
| AC08 | 원래 서버 곡·앨범·플레이리스트의 수정·삭제·재정렬 뒤 연결을 복구해도 로컬 목록·파일·대기열 불변. 자동 동기화·재다운로드 없음. |
| AC09 | 여러 서버의 같은 곡 ID가 로컬 파일을 덮어쓰지 않음. 서로 다른 서버에서 받은 음악을 하나의 로컬 대기열·플레이리스트에서 순서·중복대로 재생. |
| AC10 | 로컬 목록 생성·이름 변경·재정렬·제거가 서버에 전송되지 않음. 목록 삭제와 실제 음원 삭제가 구분되고 명시적 파일 삭제는 모든 로컬 참조를 일관되게 처리. |
| AC11 | 전송 중 연결 단절·프로세스 종료·원본 변경·Range 불일치·checksum 오류를 복구하거나 명확히 실패 처리. 불완전한 파일을 완료로 공개하지 않음. |
| AC12 | 전송 완료와 로그아웃/취소 경합에서 이미 확정된 파일 보존, 미완료 작업의 인증 재사용 차단, 부분 등록·사라진 파일 없음. |
| AC13 | 데이터 제한 없음→과금망 전환·공간 부족·저장 한도·사용자 취소를 정책대로 처리. 이미 완료된 음악 자동 삭제나 무한 재시도 없음. |
| AC14 | 묶음 일부 실패 시 저장된 곡만 명시적으로 재생. 같은 파일 재요청 시 확인된 checksum/음질 일치만 재사용하고 이름·서버 ID로 추정하지 않음. |
| AC15 | 다운로드 작업은 원래 서버·계정에 고정. 다른 서버 탐색·로그인으로 작업 주소/인증이 바뀌지 않음. 완료 뒤 원격 자격 증명 연계 없이 로컬 사용 가능. |
| AC16 | 로컬 음악을 들으며 서버 화면 탐색·로그아웃·다른 네트워크 출력 제어를 해도 로컬 재생 유지. 해당 Android 엔진 소유권 전환 때만 명시적 확인, 늦은 옛 명령의 오배송 없음. |
| AC17 | Android 잠금 화면·백그라운드·Bluetooth·오디오 포커스·헤드폰 분리·회전·재생성에서 현재 소유자에게만 시스템 제어 전달. |
| AC18 | Fold7 접힘/펼침·전화·태블릿·가로 화면·글꼴 확대·TalkBack에서 서버 선택, 저장 음악 진입, 다운로드, 플레이어와 삭제 조작 가능. 여백 중복 없음. |
| AC19 | 잘못된 TLS·Server ID·외부 redirect·임의 경로/URL·비신뢰 문서의 요청이 다운로드 경계를 우회하지 못함. 원격 Web이 로컬 음악 전체를 조회·삭제하지 못함. |
| AC20 | 같은 서명 앱 업데이트가 로컬 음악·목록·대기열·위치를 보존. 구형 Server의 기능 부족도 완료곡에 영향 없음. 앱 삭제/데이터 초기화의 별도 손실 경계를 안내. |
| AC21 | 로컬 디코딩 오류와 누락 파일은 기기에 기록. 반복 실패 무한 루프, 서버 자동 진단 업로드, NAS 자동 재접속/복구나 원본 실패 판정 없음. |
| AC22 | 실제 Android 설치·다운로드 바이트·재생 상태·원본/AAC 가청 출력 증거를 구분. UI·에뮬레이터 상태만으로 물리 재생 성공을 선언하지 않음. |
| AC23 | 곡·앨범·폴더 다중 선택과 일괄 삭제에서 실제 곡 수·파일 용량·영향 목록 표시. 목록 제거와 파일 삭제 구분, 현재 곡의 종료 후/즉시 삭제 선택, 일부 실패를 정확히 처리. 서버·다른 앱 파일 불변. |
| AC24 | 실제 로컬 폴더 생성·이름 변경·곡/폴더 이동 후 파일 배치와 UI 일치. 로컬 곡 ID·음원 checksum·태그·플레이리스트·대기열·위치 보존. 재생 중 이동에서 묵시적 재생 중단 없음. |
| AC25 | 이름 충돌·중복 선택·이동 도중 프로세스 종료·디스크 오류에서 덮어쓰기·파일 소실·거짓 완료 없음. 미완료 이동을 복구하고 폴더/DB 상태 일치. |
| AC26 | 앱 음악 루트 탈출·심볼릭 링크·하위 폴더로의 순환 이동 거부. 다운로드 목적지 폴더 이동/삭제 시 올바른 추적·대기 처리, 삭제한 폴더 자동 복원이나 완료곡 손실 없음. |

## 12. 구현 범위와 남은 검증

현재 소스에는 독립 로컬 보관함과 소유권 경계, 원본/AAC v1 Server 다운로드와 manifest, 네이티브 진입·검색·로컬 재생목록/중복 보존 대기열, 실제 비공개 폴더와 비정상 종료 복구 이동, 다운로드 관리와 과금망 정책, 검증된 로컬 확정, 삭제/현재 곡 지연 삭제, 자동 재생 없는 단일 Media3 소유권 전환이 구현되어 있다.

릴리즈 작업에서는 소스·emulator 결과를 실물 기기 결과로 바꾸어 말하지 않고 AC01–AC26을 검증하며 한·영 사용자 안내와 HTTP 계약을 일치시켜야 한다. 별도로 승인한 서명·배포 절차를 거치기 전에는 공개 production 릴리즈나 실물 기기 검증 완료를 주장하지 않는다.

Windows와 iOS 오프라인 구현, Web/PWA 오프라인 재생, 클라우드 동기화, DRM, Server의 완료 파일 원격 회수, 임의 로컬 파일 가져오기·내보내기/백업, 휴대전화 casting media server는 범위 밖이다. 데이터 마이그레이션, 병합, 서명, 물리 설치, NAS 교체와 공개는 별도 승인이 필요하다.
